{
  "summary": "Map what each installer supports against what its applier emits, to find silent gaps",
  "agentCount": 8,
  "logs": [
    "7/7 appliers audited"
  ],
  "result": {
    "summary": "# LIS APPLIER AUDIT — CONSOLIDATED MAP\n\nSeven per-applier audits merged. I independently re-verified every `lis_common.py` claim cited below (grep/read of the actual source); per-applier and per-installer claims are carried through from the individual audits with evidence quality flagged at the end.\n\n---\n\n## 1. CROSS-CUTTING — fix in `lis_common.py` or the spec, not seven times\n\n**X1. Encryption disappears when it sits under RAID or over an array. (arch, fedora, suse — 3 of 7)**\nThe single worst class in the whole audit: the operator asks for full-disk encryption and gets a plaintext disk with zero refusals.\n- `lis2archinstall.py:570` — presence of `storage.raid` takes a branch that never attaches `disk_encryption`; `backing()` resolves the container down to the bare array so the LUKS layer evaporates.\n- `lis2kickstart.py:158` — `encryption.get(handle)` is consulted only inside the *partitions* loop; the `raid` loop (`:204-214`) never emits `--encrypted`, and `:210-212` compares raw handles so the array falls through to a hardcoded `\"/\"` mountpoint.\n- `lis2agama.py:286-342` — `storage.encryption` is read only to work out LVM consumption; no `luks2` block is ever emitted into the Agama profile at all, and `check_unsupported()` warns about RAID and LVM but says nothing about encryption.\n`check_raid_consumers()` (`lis_common.py:646`) accepts encryption as a valid consumer, so nothing refuses. **Fix: a shared assertion that every `storage.encryption[]` entry produced output, run at the end of `translate()`.** This is a fail-closed guard, not seven bug fixes.\n\n**X2. All seven appliers refuse `x-*` extensions.** Verified myself: `NON_INTENT_SECTIONS = frozenset({\"lis\",\"meta\"})` (`lis_common.py:83`), `check_unhandled` (`:93-108`) refuses anything else not in `handled`, and all seven call sites pass `ALL_SECTIONS` (kickstart:553, agama:670, alpine:697, autoinstall:1020, archinstall:1037, debian:1043, nixos:1018). SPEC §2.2/§18 says extensions MUST be ignored by appliers that do not recognise them. This is actively breaking today: `lis2agama.py:487` *tells the operator to set* `x-suse.product`, and a document that does exits 1. `docs/examples/server-btrfs.lis.json` is refused by Fedora solely for carrying `x-nixos`. One-line fix.\n\n**X3. `check_unhandled(doc, ALL_SECTIONS)` is a no-op in all seven.** Same grep. Passing the complete section set means the guard can never fire on a spec section — its docstring's entire purpose (\"a new section is loud until someone teaches an applier about it\") is unfulfilled, and its only live effect is X2's incorrect refusal. Each applier should pass the sections it actually translates.\n\n**X4. `network.firewall.enabled` is never consulted. (ubuntu, debian, fedora, suse, arch, alpine — every `chroot_intents` caller)** Verified: `lis_common.py:930` is `if firewall := network.get(\"firewall\")` — truthiness of the dict. `{\"enabled\": false, \"allow_ports\": [...]}` installs and enables ufw/firewalld. On Fedora this is a proven inversion: the applier emits `firewall --disabled` *and* a `%post` that enables firewalld, and `%post` runs after Anaconda's firewall task, so the explicit `enabled: false` loses.\n\n**X5. `registration.server` / `registration.email` are consumed and thrown away.** Verified: `REGISTRATION` (`lis_common.py:832-835`) — fedora's template has `{org}` but no `{email}`, suse's has `{email}` but no `{org}`, ubuntu's `\"pro attach $TOKEN\"` has neither. `:859-861` formats both in regardless, so `.get()` marks them read and the tracker stays silent. All three targets can express both (`--email`, `SUSEConnect -e`, `pro config set contract_url`).\n\n**X6. The access tracker is structurally blind to per-item drops.** `declared_paths()` (`lis_common.py:473-488`) collapses list indices, so reading `users[0].ssh_authorized_keys` marks `users[].ssh_authorized_keys` read for *every* user. This is what hides alpine A5/A6 (one SSH key, one admin), debian A5 (secondary users' `comment`), fedora A5 (everything on a `root` entry), suse A5 (one key total). Not a bug in the tracker, but the maintainer should stop treating a clean `check_unread` as coverage for list-of-object sections.\n\n**X7. Script lifecycle stages are collapsed into one, unannounced. (ubuntu, debian, suse, nixos — 4)** `post_storage`, `post_install`, `post`, `pre_reboot`, `on_success` all become one bucket: autoinstall `late-commands` (`lis2autoinstall.py:749-755`), d-i `preseed/late_command` (`lis2debian.py:925-928`), AutoYaST `chroot-scripts` (`lis2agama.py:108-110`), NixOS `system.activationScripts` (`lis2nixos.py:665-673`). SPEC §13 gives each a distinct contract — `post_storage` runs in the live installer right after formatting, `pre_reboot` after unmount, `on_success` only on a clean finish. None of that survives and no applier warns. Two of the four targets have a native home for at least `post_storage` (Anaconda `%pre-install`, AutoYaST `postpartitioning-scripts`, Agama `scripts.postPartitioning`) — see §4.\n\n**X8. Btrfs subvolume names are mangled by `lstrip(\"@\")`. (fedora, suse, nixos — 3)** Verified by grep: `lis2kickstart.py:201,271` (`or \"root\"`), `lis2agama.py:334,478` (`or \"@\"`), `lis2nixos.py:262,580`. `@home` lands on disk as `home`; `@` becomes `root` on Fedora and stays `@` on SUSE, so siblings from one declaration come out under two conventions. `lstrip` also strips *all* leading `@`. Nothing in btrfs, Anaconda (`custom_partitioning.py:1155-1196`) or blivet requires this. debian, ubuntu, arch, alpine do not mangle — so this is three appliers disagreeing with the other four about what the document said.\n\n**X9. `role_mountpoint()` synthesises mountpoints the document deliberately omitted. (fedora, suse, nixos — 3)** `lis_common.py:61-62` falls back to `ROLE_MOUNT`. On `docs/examples/test-raid.lis.json` — a **passing** e2e recipe — the mirrored `boot2` partition has `role: boot` and no mountpoint, and gets `/boot` anyway: two `part /boot` lines on Fedora (Anaconda then `destroy_device`s the first), two `\"path\": \"/boot\"` entries in the Agama profile, two `mountpoint = \"/boot\"` in disko. The AutoYaST renderer has a `claimed_mounts` dedup (`lis2agama.py:353,445-448`); the Agama renderer in the same file does not.\n\n**X10. `system.security.module` installs a package and stops. (ubuntu, suse, arch — 3)** `security_packages()` (`lis_common.py:704-720`) adds `apparmor`/`selinux-policy` and nothing enables the LSM: no `lsm=`/`apparmor=1` kernel parameter, no unit enabled, no SELinux relabel. On Ubuntu it is worse — `lis2autoinstall.py:674-680` calls `security_packages()` only in the `else` branch of an **nvidia GPU check** (verified), so with `gpu: nvidia` the LSM request vanishes entirely.\n\n**X11. Flatpaks are installed from a remote that was never added. (ubuntu, fedora, debian — 3)** `flatpak install -y flathub <app>` with no preceding `flatpak remote-add --if-not-exists flathub`. On Ubuntu the `flatpak` package ships no remotes, so every declared flatpak silently fails in cloud-init `runcmd`; on Fedora it is under `%post --erroronfail`, so it may abort the install. arch drops `apps[].flatpak` and `software.flatpak[]` on the floor entirely (`lis2archinstall.py:531-533`).\n\n**X12. `desktop.display_manager: \"none\"` is refused although it means \"install no DM\".** `DISPLAY_MANAGERS` in `lis_common.py` has no `none` entry, so `chroot_intents` (`:911-913`) refuses a schema-legal enum value on every distro that uses it. Kickstart's `skipx` says exactly this; Alpine satisfies it by doing nothing.\n\n**X13. `check_section_fields(doc, \"desktop\", ...)` warns that `display_manager` \"is not applied by this applier\" while `chroot_intents()` applies it. (fedora, suse, and the equivalent false `check_boot_extras` warnings in nixos)** A warning channel that cries wolf on every desktop document trains operators to ignore the one signal they have.\n\n**X14. `installer.on_finish` defaults to `reboot`; SPEC §16 says `stay`. (debian `:1015`, suse `:368`)** Two appliers reboot a machine on a document that said nothing, and both would have *refused* `stay` if it had been written down.\n\n---\n\n## 2. SILENT DROPS — ranked\n\n| # | Distro | Field | Now | Should |\n|---|---|---|---|---|\n| 1 | arch / fedora / suse | `storage.encryption` under RAID/LVM | plaintext disk, 0 refusals (X1) | emit, or refuse the combination |\n| 2 | alpine | `storage.swap.*` | `mkswap` runs; `SWAP_DEVICES` is never set, so swap is absent from fstab and never `swapon`ed — **in every recipe in the repo** | set `SWAP_DEVICES`; e2e verifier has no swap check |\n| 3 | alpine | any `mountpoint` other than `/` and `/boot` | partition/LV is `mkfs`'d and never mounted, so `genfstab` never sees it (`:573-598`). Hits `server-lvm-pool.lis.json` and any `/home`/`/var` | mount before `genfstab` |\n| 4 | nixos | `role: root` / `role: boot` with no explicit `fs` | partition emitted with **no `content`**, no `fileSystems.\"/\"`, unbootable, `0 warnings 0 refusals` (`:199-200`, `:519`) | use `ROLE_FS`/`ROLE_MOUNT` like the other six |\n| 5 | debian | `storage.partitions[].disk` | never emitted as `device{ }`; auto-lvm puts every device-less PV on disk 1 and gives extra disks a synthesised full-disk PV. Reproduced. `server-lvm-pool.lis.json` is this shape | emit `device{ }` per `partman-auto-recipe.txt:255-264` |\n| 6 | debian | `role: esp` | emits generic `method{ format }` instead of `method{ efi }`, so UEFI installs stall on `partman-efi/no_efi` and get no `/boot/efi`. **Untested path** — every e2e recipe is BIOS | emit `method{ efi }` |\n| 7 | ubuntu | `software.role: minimal` / `server` | falls off the end of `role_packages` (`:637-644`); `role` is read so tracker is silent. Autoinstall has the native key `source: {id: \"ubuntu-server-minimal\"}` | emit `source`; schema.md:416-419 says MUST fail otherwise |\n| 8 | fedora | `software.services.enable` | emitted, then **parsed away**: two `services` lines, and pykickstart's `set_to_obj` makes the second overwrite `enabled` wholesale. Every unit lost unless `sshd` is in the list | merge into one `services` line |\n| 9 | fedora | `network.ssh.permit_root: \"prohibit-password\"` | mapped to `rootpw --allow-ssh`, which writes `PermitRootLogin **yes**`. `\"no\"` writes nothing, leaving key-login enabled | security inversion — fix or refuse |\n| 10 | ubuntu | `network.interfaces[].dns` | applier reads `iface.get(\"nameservers\")` (`:908`); the schema field is `dns`. No document can ever populate netplan nameservers | rename |\n| 11 | ubuntu | `users[].shell` | `chsh -s /usr/bin/zsh` runs but `shell_packages()` is **never imported** — verified, autoinstall is the only applier that omits it. Account gets a login shell that does not exist; the per-user firstboot hook then fails | import and call it |\n| 12 | suse | `boot.loader` | `check_boot_extras` lists it as handled; the only consumer picks *where to write the timeout*. GRUB installs regardless of `systemd-boot` | emit `<bootloader><loader_type>` |\n| 13 | debian | root's `ssh_authorized_keys` / `dotfiles` | written to `/home/root` — verified in `lis_common.py:743` (`chown -R … /home/{name}`) and `:951` (`home = f\"/home/{user['name']}\"`) | special-case root → `/root` in lis_common (fixes it everywhere at once) |\n| 14 | debian | non-primary users' keys and `scripts.post_install` | emitted **before** the `useradd` loop; `install(1)` fails, and preseed-common only checks the last command's status, so it is swallowed | reorder |\n| 15 | fedora | everything on a `users[]` entry named `root` except `password.hash` | `continue`d past at `:338-339`; `sshkey --username=root` is legal kickstart | emit |\n| 16 | ubuntu | `software.services.enable/disable` | no home in `translate()` or `chroot_intents()`; the applier already emits dozens of `systemctl` in-target commands | emit |\n| 17 | ubuntu | `system.security.module` when `gpu: nvidia` | see X10 | restructure the branch |\n| 18 | nixos | `software.exclude`, `users[].dotfiles` | `pass  # honored by chroot_intents()` at `:916-917` and `:875-876` — **verified: lis2nixos never imports or calls `chroot_intents`**. Comment is dead; `software.exclude` produces no message at all | `environment.defaultPackages` exists |\n| 19 | suse | `system.keymap.variant` | read on the Agama path only; `autoyast.xml` (the profile that actually boots) emits `<keymap>cz</keymap>` for `cz(qwerty)` | emit in both |\n| 20 | suse | btrfs subvolume `mountpoint` | AutoYaST emits `<path>` only, so `{\"name\":\"data\",\"mountpoint\":\"/var/lib/data\"}` mounts at `<btrfs-root>/data` | emit the mount |\n| 21 | alpine | ESP + separate `/boot` | `next(... in (\"/boot\",\"/boot/efi\"))` picks the ESP, mounts it at `/boot`, abandons the declared `/boot` | disambiguate |\n| 22 | alpine | the entire storage/boot description on the simple path | `BOOTLOADER`/`USE_EFI`/`DISKLABEL`/`KERNELOPTS`/`ROOTFS`/`BOOTFS` are computed and dropped; setup-disk uses its own defaults (`ext4`, `dos`, syslinux) | export them from the `--apply` driver |\n| 23 | nixos | `storage.raid[].spares` | put into the active member map and documented away with a Nix **comment** claiming \"disko marks them by device order\" — false; `mdadm.nix` has no spare option, so a 2+1 becomes a 3-way mirror | refuse |\n| 24 | arch | `drivers.gpu: amdgpu` | `gfx_driver()` keys on `\"amd\"`; schema enum is `amdgpu` → `\"gfx_driver\": null` silently. (`nvidia`/`nvidia-open` are worse — they emit strings archinstall's enum rejects, `sys.exit(1)`) | fix the key |\n| 25 | arch | `software.role: \"desktop:gnome\"` | emits `\"Gnome\"`; archinstall's profile is `'GNOME'` and lookup is exact string compare — miss is `info()`-logged and the install continues with **no desktop**. Only `gnome` is broken; `Kde` survives on a back-compat rewrite | fix the string |\n| 26 | ubuntu | `scripts.post_storage[].chroot`, `users[].scripts.post`, `network.hosts` (when interfaces are also declared), `dhcp6` | dropped; `check_script_fields(honors_chroot=True)` actively suppresses the chroot warning | — |\n| 27 | debian | `system.time.servers`, `network.ssh.enabled: false`, root subvolume `mount_options`, `users[1:].comment` | see debian A5-A8 | — |\n| 28 | alpine | one SSH key / one admin only, `users[].comment` via a nonexistent `chfn`, `luks1`→luks2, LVM volume with no `fs`, `size: \"50%\"` computing to 0 | see alpine A5-A10 | — |\n\nAlso: `storage.encryption[].type: luks1` is silently satisfied as LUKS2 by **debian and alpine** (debian: partman-crypto calls `luksFormat` with no `--type`; alpine hardcodes `--type luks2`). That is an on-disk-format request — it should refuse, not warn.\n\n---\n\n## 3. DRIFT — ranked\n\n1. **alpine: `usermod` does not exist on Alpine, and `lis-post.sh` runs under `set -e`.** Emitted at `:266` and `:309`. `lis_common.uid_commands` already documents this fact (\"no shadow package, so there is no usermod applet\") and lis2alpine does not use it. A root password hash or a second user aborts the post script *there*, so users, files, sudoers, hooks and the birth certificate are all skipped. The e2e recipe has neither.\n2. **alpine: `sh -c {json.dumps(...)}` double-quoting** at `:309,323-342`. Script bodies get `$(...)`/`$VAR`/backticks expanded **on the installer host** before the chroot; `usermod -p \"$6$salt$XX\"` expands to an empty password field; the `enrollment_commands` line interpolates the LUKS passphrase into an argv. `prepare_script` uses `shquote` and firstboot uses base64 — the inconsistency is internal.\n3. **nixos: two ways to destroy disks and then fail to evaluate.** `boot.kernel.variant: realtime` emits `pkgs.linuxPackages_rt`, which does not exist in nixpkgs 25.05 (the attribute is `linuxPackages-rt`; verified by `nix-instantiate --eval`). Package names are interpolated unquoted, so `\"gtk+\"` is a parse error. Both fail at `nixos-install`, i.e. **after** `disko --mode disko` has already wiped the disks.\n4. **nixos: partition labels diverge between `disko.nix` and `hardware.nix`.** `existing` partitions are skipped before the index increment in one renderer and not the other, so `swapDevices` points at `…-swap3` which never exists; and disko sha256-hashes any label over 36 chars while the applier hardcodes the unhashed form. Both reproduced, both `0 warnings 0 refusals`, both unbootable.\n5. **ubuntu: `storage.wipe: false` — the schema default — emits invalid curtin.** `\"wipe\": \"preserve\"` is not one of curtin's values, and the disk gets `preserve: true` while every partition under it gets `preserve: false`. Every recipe in `docs/examples/` sets `wipe: true`, so the default path is untested.\n6. **suse: `storage.wipe: false` still destroys the disk.** `<use>all</use>` is emitted unconditionally, while the Agama renderer in the same file honors it — one document, two outputs that disagree about whether the disk is erased. (alpine warns and then partitions anyway; SPEC §6.1 says MUST fail.)\n7. **ubuntu: a declared keyfile unlock is silently converted to a boot-time passphrase prompt.** Third crypttab field rewritten to `none` at `:814-816` with a code comment and no `warn()`.\n8. **debian: `passwd/user-default-groups` replaces Debian's default group list.** `groups: [\"docker\"]` *removes* audio/video/plugdev/netdev/cdrom/lpadmin from the primary account. Secondary users are fine (`useradd -G` is additive).\n9. **debian: `boot.os_prober` is written to a debconf key that is never read.** `grub-installer/with_other_os` is only asked in a branch the applier's own `bootdev` answer makes unreachable; the real knob is `grub-installer/enable_os_prober_otheros_{yes,no}`. Both values of `os_prober` are therefore no-ops.\n10. **debian: `mirror.url` loses its scheme and is overridden by country.** `mirror/protocol` is never emitted so `https://…` installs over plain http, and `mirror/country` is emitted with the real country code where the official preseed uses `manual` alongside an explicit hostname.\n11. **fedora: duplicate `part /boot` from `role_mountpoint` → Anaconda destroys the first one.** In a passing e2e recipe (X9).\n12. **arch: `boot.uki: true` is emitted as `\"uki\": false`** (hardcoded `:559`) — the profile asserts the opposite of the document, on a field archinstall fully supports.\n13. **arch: zram is switched on for documents that never mention swap.** `bool((storage.get(\"swap\",{}) or {}).get(\"zram\", True))` → `True` with no `storage.swap` key at all. archinstall's own default is `None`.\n14. **alpine: an invented serial console that also clobbers what setup-disk wrote.** `GRUB_TERMINAL=\"serial console\"` + ttyS0/115200 hardcoded, `/etc/default/grub` rewritten wholesale (losing `GRUB_TIMEOUT`, submenu/recovery settings), while `check_boot_extras` warns that `boot.console` \"is not applied\".\n15. **alpine: `admin: false` still puts the primary user in wheel** (`-g wheel` unconditional at `:224`); and `sudo: nopasswd` writes a sudoers drop-in into a system that has doas and no sudo — the e2e verifier accepts the drop-in file as evidence.\n16. **ubuntu: `software.apps[]` invents an apt package from the display name.** `{\"name\":\"obs\",\"flatpak\":\"…\"}` yields an apt package `obs` *and* a flathub command; `apps[].snap/appimage/preference` are never read. Snaps are emitted twice, the second as `snap install {'name': 'lxd', …}` — a Python repr in a shell command, saved only by `|| true`. `display_manager: \"gdm\"` emits `apt-get install -y gdm` without `|| true`; Ubuntu's package is `gdm3`, which the LIS enum does not permit.\n17. **fedora: `desktop.autologin` always writes `/etc/gdm/custom.conf`** regardless of `display_manager: sddm`.\n18. **nixos: `${` is not escaped in `nix_str`** — file contents, comments, mount options and hostnames can open Nix antiquotation. `nix_script` escapes correctly, so hook bodies are safe and `files[].content` is not. Also `password.locked: true` discards the hash entirely (`lis_common.password_field` returns `!<hash>`; nixos returns `\"!\"`).\n19. **suse: `on_finish: poweroff` becomes \"stay\"**, and nothing about `on_finish` reaches the Agama profile at all. Same file: Agama defaults the product to Tumbleweed while AutoYaST defaults to Leap — one document, two distributions, one warning.\n20. **alpine: NVMe partition nodes are wrong** (`f\"{device}{index}\"` → `/dev/nvme0n11`), and a `rest` LVM volume declared before a sized one makes the next `lvcreate` fail.\n21. **arch: `network.manager: systemd-networkd` becomes \"copy the ISO's network config\"** — declared interfaces are gone and the warning does not say so.\n22. **fedora: `--iscrypted` + `--lock` yields `!!<hash>`** in shadow (Anaconda prepends its own `!`). Cosmetic but wrong.\n\n---\n\n## 4. UNUSED CAPABILITY WORTH ADDING\n\nOrdered by how much declared intent it unblocks.\n\n- **Wrong refusals that block valid documents.** Fedora refuses `scripts.on_error` — `%onerror` exists and Anaconda runs it. Ubuntu refuses it too — autoinstall has `error-commands`. Ubuntu refuses `network.wifi` as \"not expressible\" — autoinstall has `wifis:` with netplan `access-points[].password` taking a 64-hex PSK. Debian says the same and has `netcfg/wireless_essid` + `netcfg/wireless_wpa`. Alpine says the same and has `WIFIOPTS` → `setup-interfaces -w`. Arch refuses `snapshots.tool: timeshift`, which archinstall supports natively; and refuses the whole `mirror` section, which archinstall's `mirror_config.custom_servers` covers. These are the cheapest wins: the capability exists, the LIS field exists, and today the document is rejected.\n- **`scripts.post_storage` has a real home in three installers** — Anaconda `%pre-install` (\"filesystem mounted, no packages\" — verbatim the LIS contract; the Fedora applier's code comment claiming otherwise is wrong), AutoYaST `postpartitioning-scripts`, Agama `scripts.postPartitioning`. Fixes a third of X7.\n- **`network.interfaces[]` static config is expressible everywhere and emitted nowhere.** Debian: `netcfg/disable_autoconfig` + `get_ipaddress`/`get_netmask`/`get_gateway`/`get_nameservers`. Fedora: `network --bootproto=static --ip= --netmask= --gateway= --nameserver=`. Arch: `network_config.nics[]`. NixOS: `networking.interfaces.<n>` or `systemd.network` (which also handles `match.name: \"en*\"` globs and `match.mac`, both currently refused). AutoYaST `<networking><interfaces>`. Alpine `INTERFACESOPTS`. Every single distro can do this; only netplan-on-Ubuntu emits anything, and it emits the glob as a literal device id.\n- **`network.interfaces[].match.name` on Debian → `netcfg/choose_interface`.** Without it a multi-NIC machine *prompts mid-install*, which is a hang, not a drop.\n- **`boot.password_hash`** — GRUB has it on every distro (`grub-installer/password-crypted`, kickstart `bootloader --password --iscrypted`, `boot.loader.grub.users.<n>.hashedPassword`). schema.md:283 says an applier whose loader lacks the concept MUST fail; every applier here has the concept and warns-and-drops instead.\n- **`storage.partitions[].mount_options` and `label`** — partman `options/<opt>{ }` and `label{ }`, kickstart `--fsoptions` / `--label`, AutoYaST `<fstopt>`/`<label>`, disko `filesystem.extraArgs`. Currently warned on four appliers.\n- **Native firewall wiring on arch** — `app_config.firewall_config` installs, enables *and* flips `ENABLED=no`→`yes` in `/etc/ufw/ufw.conf`. The hand-rolled `chroot_intents` version leaves ufw enabled-but-inactive. Same file: `app_config.bluetooth_config` / `print_service_config` for `desktop.bluetooth` / `printing`.\n- **Alpine's community repository.** `APKREPOSOPTS=\"-1\"` leaves community commented out, but `firefox`/`gnome`/`plasma`/`xfce4` — which the applier itself installs — live there, so `apk add` fails and `set -e` takes the rest of the post script with it. Also `setup-desktop` already wires DM + dbus/elogind + a display stack, including `sway`, which the applier refuses.\n- **`system.time.*`** — kickstart `timesource --ntp-server/--ntp-pool/--ntp-disable`, cloud-init `ntp:` on Ubuntu, AutoYaST `<ntp-client>`. Currently unread on ubuntu, fedora, suse; on Debian it is emitted to a key that only drives the installer's own clock and is then overwritten by DHCP.\n- **`users[].ssh_authorized_keys` natively** — AutoYaST `<user><authorized_keys>`, kickstart `sshkey --username=` (including for root). Would fix suse A5 and fedora A5 at once.\n- **`software.services.disable[]`** — refused by nixos (`systemd.services.<n>.enable = false` works), unemitted by ubuntu/arch, both of which already emit arbitrary `systemctl` commands.\n- **`storage.partitions[].existing`** — refused by fedora (\"not expressible in an unattended kickstart\": `part --onpart=UUID=… --noformat` and the `mount` command both exist), refused by suse on Agama's behalf although AutoYaST has `<create>false</create>`/`<partition_nr>`/`<resize>`, and warned-and-skipped by nixos where it produces the label divergence in Drift #4. Debian and Ubuntu can do most of it too (curtin `preserve: true` + `number`).\n- **Ubuntu's modern apt block** — `apt.mirror-selection` / `geoip` (for `mirror.country`) and `disable_components`; without the latter, `driver_packages` emits Ubuntu GPU/firmware names that live in `restricted`/`multiverse` with nothing enabling them.\n- **`system.keymap.font`** — archinstall `locale_config.console_font` (auto-straps `terminus-font`), console-setup `console-setup/fontface47` on Debian.\n- **NixOS specifically**: `networking.wireless.networks.<ssid>.pskRaw`+`.hidden`, `boot.uki.enable`, `pkgs.linuxPackages_zen` (verified to evaluate; currently refused), `zramSwap.memoryPercent`, `networking.proxy.httpsProxy`, `environment.defaultPackages`, all five display managers. Nine refusals/drops that are one option each.\n\n---\n\n## 5. SPEC GAPS — the installer can, the schema cannot say it\n\n**Appearing in three or more distros (worth adding to the schema):**\n- **Partition-table type.** `clearpart --disklabel=`, `partman-partitioning/default_label`, disko `table.nix` msdos, Alpine `DISKLABEL`. `target.firmware` is the closest field and it does not answer \"GPT on a BIOS box\" — which is exactly when a `biosgrub`/`EF02` partition is mandatory, so three appliers invent one.\n- **Extra repositories / repository components.** Fedora `repo`, Debian `apt-setup/local0/*` + `services-select`, Arch `optional_repositories`(multilib)/`custom_repositories`, Alpine community/testing, SUSE `<add-on>`, Ubuntu `apt.sources`. schema.md:434-438 deliberately parks this in `x-*` — but `x-*` is refused by all seven (X2), so the escape hatch does not exist. And it has a real cost: `driver_packages` must *refuse* `drivers.gpu: nvidia` on Fedora/SUSE purely because RPM Fusion is one `repo` line away and unsayable.\n- **LUKS parameters.** cipher/keysize/PBKDF/iter_time (`partman-crypto/{cipher,keysize,ivalgorithm,keyhash}`, Agama `luks2.{cipher,keySize,pbkdFunction}`, archinstall `iter_time`, kickstart `--luks-version`). `storage.encryption[]` has only `type`/`key`/`unlock`. Related: `unlock[]` is an ordered list with no way to attach distinct key material per entry (disko `additionalKeyFiles`).\n- **fstab device-naming policy.** `partman/mount_style`, AutoYaST `<mountby>`. No LIS field.\n- **Package-set policy.** `pkgsel/upgrade`, `base-installer/install-recommends`, `%packages --excludeWeakdeps/--nocore/--ignoremissing`, `archinstall` profile sub-selections. `software.role` is far coarser; \"genuinely minimal\" is unsayable.\n- **Btrfs default subvolume.** AutoYaST `<btrfs_default_subvolume>`. LIS encodes the `@` convention *inside subvolume names*, which is precisely what invites the `lstrip(\"@\")` mangling in X8. A first-class field removes the guess.\n- **Installer-side remote access.** `network-console/{password,authorized_keys_url}`, `sshpw --username=`. `installer.*` has no vocabulary for it.\n- **Subvolume `mount_options`** — LIS declares them and neither kickstart's `btrfs` command nor archinstall's `SubvolumeModification` nor disko can express them. Two-sided; the current warnings are correct.\n\n**Single-distro, but flagged because they force an applier's hand today:**\n- **SUSE base product.** AutoYaST `<software><products>` / Agama `product.id` are effectively mandatory, LIS has no field, so the applier is forced into `x-suse.product` — which X2 then refuses. **This is the one gap actively breaking an install path.**\n- **Alpine diskless/lbu/apk-cache** (`LBUOPTS`, `APKCACHEOPTS`, `setup-disk -m data`) — Alpine's defining install modes, hardcoded to `\"none\"`. Plus device manager (`setup-devd mdev|mdevd|udev`, which the hardcoded `eth0` silently depends on), doas (`users[].sudo` is a sudo-shaped enum), and kernel flavors (`virt` — the right choice for the QEMU targets this repo tests — cannot be requested; `boot.loader` cannot say `syslinux`/`limine`/`none` although the applier's own map lists them).\n- **NixOS / disko**: ZFS pool topology (schema §6.1 already admits `fs: zfs` is single-filesystem only in v0.1 — this is why `render_zpools` invents `rpool` and guesses `mirror`), swap tuning (`priority`, `randomEncryption`, `resumeDevice`), LVM thin pools / RAID LVs, mkfs tuning, disk-image build (`imageName`/`imageSize`).\n- **Ubuntu**: `codecs`, `updates: security|all`, `refresh-installer`, `reporting` webhooks, `oem`, `active-directory` (note `system.domain` means DNS domain — mapping it to an AD realm would be an invention), `storage.layout` shorthand.\n- **Arch**: `plymouth` themes, `auth_config.u2f_config` (LIS `keys[].purpose: user_pam_auth` exists with no way to bind a login method), fonts/power-management app configs, zram `algorithm` (LIS has `zram.size`, which archinstall lacks, and lacks the algorithm it has).\n- **Fedora**: DNF `module`, `authselect`/`realm join`, `user --homedir/--gid`.\n- **Debian**: EFI fallback bootloader (`force-efi-extra-removable`, `update-nvram`), `netcfg/target_network_config`.\n\n---\n\n## Evidence quality — read before acting\n\n**Strongly grounded** (installer source read directly, claims reproduced): debian (bookworm udebs fetched and cited by file:line, five reproductions), nixos (disko + nixpkgs from the store, `nix-instantiate --eval` and `--parse` used to confirm the two eval failures and `t.check \"50%\" == false`), arch (archinstall 4.4 from the ISO, enum members quoted), alpine (real Alpine tree, nine documents run), fedora (pykickstart/pyanaconda/blivet from `install.img`, output re-parsed with the real F41 handler).\n\n**Verified by me in this pass**: every `lis_common.py` claim above — `NON_INTENT_SECTIONS`, `check_unhandled` call sites in all seven, the firewall truthiness test, the three `REGISTRATION` templates and their missing placeholders, `declared_paths` index collapse, `role_mountpoint`, `password_field`, the `lstrip(\"@\")` sites, lis2nixos never importing `chroot_intents` despite two comments claiming otherwise, and lis2autoinstall omitting `shell_packages`/`file_commands` from its import.\n\n**Weak or explicitly unverified — do not act on these without checking:**\n- **fedora**: comps was not available, so `@server-product`, `@gnome-desktop`, `@kde-desktop`, `@xfce-desktop` are unvalidated group ids, as is the refusal of `desktop:sway` (Fedora ships a Sway spin, so that refusal may be unnecessary). The kdump `%addon` was not confirmed present. The `%post --erroronfail` flatpak abort was reasoned, not run.\n- **alpine**: alpine-conf was inspected at **3.22.0-r0**; the e2e ISO is **3.21.3**. Every `setup-*` claim is version-shifted by one release.\n- **arch**: the `role: esp` → `/boot/efi` bootloader outcome (Drift, arch B4) was reasoned from `get_boot_partition`/`get_efi_partition` and not run — flagged by that audit as unproven.\n- **suse**: three findings were self-flagged unverified — whether YaST still adds an LVM swap volume to fstab without `<mount>`, whether YaST rejects raw X keymap codes outside the 7-entry `YAST_KEYMAP` table, and whether omitting `size` in an Agama profile really hands sizing to product defaults. The AutoYaST/Agama element names come from published docs, not from a running installer.\n- **ubuntu**: the autoinstall match-key list (which excludes `wwn`), the `error-commands` schema entry, and the `gdm`-not-a-package claim come from the autoinstall reference and packages.ubuntu.com, not from subiquity source. The curtin `wipe` enum likewise. Directionally I trust them; confirm against subiquity before changing the `match` builder.\n- **Line references into `spec/schema.md` and `spec/schema.json`** are carried through from the individual audits and I did not re-check them. The *semantic* claims they support (§2.3 refuse-or-emit, §13 script stages, §16 `on_finish` default `stay`, §18 `x-*` ignored) are consistent across all seven audits, which is decent corroboration; the specific line numbers are not.\n\n**One claim I would demote outright**: the arch audit's C-table entry saying `mirror_config.mirror_regions` \"KeyErrors on an unknown country\" is inference from reading `regions_config`, not a run. Everything else in that table is cited to a source line.",
    "audits": [
      "I read the applier, the spec, `lis_common.py`, ran it against the e2e recipes and a maximal document, and checked the subiquity autoinstall schema + curtin config docs. All line refs are `/home/bresilla/lis/tools/appliers/lis2autoinstall.py` unless noted.\n\n---\n\n# A. SILENT DROPS — declared, neither emitted nor refused\n\n**A1. `software.role: \"minimal\"` / `\"server\"` — completely silent.**\n`role_packages` (`:638-640`) only holds `desktop:*` keys. `:641-644` appends a package for a desktop role and refuses an unknown `desktop:*`, but any non-desktop role falls off the end. `role` *is* read (`software.get(\"role\",\"\")` at `:637`), so the tracker is satisfied and nothing is printed. Verified: `docs/examples/test-full-install.lis.json` declares `\"role\": \"minimal\"` and the only warning the run produces is about kernel params; the generated `user-data` has no `source:` key and no role-derived package. schema.md:416-419 says appliers \"MUST fail on roles they cannot provide\". Autoinstall has the exact native key: `source: {id: \"ubuntu-server-minimal\"}` (confirmed in the autoinstall reference). This is the single clearest silent drop.\n\n**A2. `system.security.module` is dropped whenever `drivers.gpu` is nvidia.**\n`:674-680`:\n```python\nif drivers.get(\"gpu\") in (\"nvidia\", \"nvidia-open\"):\n    auto[\"drivers\"] = {\"install\": True}\n    packages += driver_packages(doc, \"ubuntu\", skip=frozenset({\"gpu\"}))\nelse:\n    packages += driver_packages(doc, \"ubuntu\")\n    packages += security_packages(doc, \"ubuntu\")   # <- only in this branch\n```\n`security_packages()` is the *only* consumer of `system.security.module`, so with an nvidia GPU the LSM request vanishes: no apparmor/selinux package, no refusal. Confirmed on my max document — the run emitted `system.security.module is declared but this applier never reads it`, which is the generic tracker line, not a decision. With `gpu: amdgpu` the same document installs `apparmor`. A branch of an unrelated `if` decides whether a security control is honored.\n\n**A3. `users[].shell` names a shell that is never installed.**\n`:767-769` runs `chsh -s /usr/bin/zsh <user>` in-target, and `shell_path()` (`:866`) invents `/usr/bin/<name>` for a bare name. `shell_packages()` from `lis_common` is never imported or called — lis2autoinstall is the **only** applier that omits it (`lis2alpine.py:240`, `lis2agama.py:169`, `lis2kickstart.py:417`, `lis2archinstall.py:524`, `lis2debian.py:796` all call it). schema.md:355 — \"Intent names oblige the applier to install the shell.\" Because `shell` *is* read, the tracker says nothing. Result: `shell: zsh` produces an account whose login shell does not exist, and the per-user `firstboot` hook (`su - <user> -c …`, `:840`) fails with it.\n\n**A4. `users[].scripts.post` is dropped.**\n`:793-797` iterates only `post_install`; `:838` only `firstboot`. `scripts.post` is in the schema (`spec/schema.json:738`). Confirmed: `users[].scripts.post[].content is declared but this applier never reads it`. (Top-level `scripts.post` *is* handled at `:749`; only the per-user variant is missing.)\n\n**A5. `software.services.enable` / `disable` — no home anywhere.**\nNot in `translate()`, not in `chroot_intents()` (`lis_common.py:887-963` handles exclude/snap/display_manager/manager/firewall/dotfiles/snapshots — no services). schema.md:428 makes systemd unit names the lingua franca, and the applier already emits dozens of `curtin in-target -- sh -c \"systemctl …\"` commands, so this is honorable, not impossible. Surfaces only as the generic tracker warning.\n\n**A6. `network.interfaces[].dns` is read under the wrong name.**\n`:908` reads `iface.get(\"nameservers\")`; the schema field is `dns` (`spec/schema.json:801`, schema.md:371). No LIS document can ever populate netplan `nameservers`. Confirmed: `network.interfaces[].dns is declared but this applier never reads it`.\n\n**A7. `network.interfaces[].dhcp6` dropped.** Never read (`:900-910`); netplan supports `dhcp6` directly.\n\n**A8. `network.hosts` warns on only one code path.**\nThe `warn()` at `:890` sits inside `if not interfaces:` (`:883`). Declare interfaces *and* hosts and the warning never fires — only the tracker's generic line does. Either way `/etc/hosts` is never written, though a late-command trivially could.\n\n**A9. `network.firewall.enabled: false` is ignored, and the firewall is installed anyway.**\n`lis_common.py:930` does `if firewall := network.get(\"firewall\")` — a truthy dict. `enabled` is never consulted, so `{\"enabled\": false, \"allow_ports\": [...]}` still emits `apt-get install -y ufw` + `systemctl enable ufw`. Confirmed by the tracker line `network.firewall.enabled is declared but this applier never reads it` while the output at my `max/user-data:177-183` contains the ufw install.\n\n**A10. `scripts.post_storage[].chroot` is silently ignored.**\n`:723-725` appends the raw content string (host context, correct default per schema.md:§13.2). But `check_script_fields(doc, honors_chroot=True)` at `:1042` tells the checker this applier honors `chroot` everywhere, so `post_storage` with `chroot: true` is dropped with no word. Same for `scripts.pre`/`pre_install` (`:711-714`).\n\n**A11. `x-<distro>` extensions are refused instead of ignored — including this applier's own namespace.**\n`check_unhandled(doc, ALL_SECTIONS)` at `:1020` is passed the *whole* section set, so it can never fire on a spec section; the only thing it ever catches is `x-*`. Verified: a document with `\"x-ubuntu\": {...}` produces `refused: section 'x-ubuntu' is declared but this applier does not translate it`. schema.md:§18 says extensions are \"passed verbatim to appliers that recognize it and **ignored by all others**\". This is the inverse of a silent drop but the same class of defect: the guard is mis-wired.\n\n---\n\n# B. DRIFT — emitted config that differs from what was declared\n\n**B1. `storage.wipe: false` (the schema default) emits an invalid curtin action.**\n`:168-171`:\n```python\n\"wipe\": \"superblock-recursive\" if wipe else \"preserve\",\n\"preserve\": not wipe,\n```\nCurtin's documented `wipe` values are `superblock`, `superblock-recursive`, `pvremove`, `zero`, `random` — `preserve` is not one of them; `preserve` is a separate boolean. Verified output (`t2/user-data:22-24`): `wipe: preserve` / `preserve: true` on the disk, while every partition beneath carries `preserve: false` — i.e. \"don't touch this disk, but create partitions on it\". **Every** recipe in `docs/examples/` sets `wipe: true`, so this entire branch is untested and it is the default a document gets by omitting the key.\n\n**B2. `target.disks[].match.wwn` is emitted as a subiquity match key that does not exist.**\n`:139-141` copies `serial`, `model`, `wwn` straight into the `match` directive. The autoinstall reference lists exactly `model, vendor, path, id_path, devpath, serial, ssd, size, install-media` — no `wwn`. The autoinstall JSON schema for `storage` is bare `{\"type\": \"object\"}`, so nothing validates it. `match_selectors()` (`lis_common.py:689-701`) only warns when `match.path` is *also* present, so a wwn-only selector is silent. A document that pins a disk by WWN gets a matcher that either drops the constraint or crashes subiquity.\n\n**B3. A glob interface name becomes a literal netplan device id.**\n`:896` takes `iface[\"match\"][\"name\"]` and `:910` uses it as the `ethernets:` key. schema.md:368 gives `{\"match\": {\"name\": \"en*\"}}` as the canonical example. Verified output:\n```yaml\nethernets:\n  en*:\n    dhcp4: true\n```\nNetplan matches ids literally unless a `match:` block is given, so this configures nothing. `match.mac` is refused rather than mapped to netplan's `macaddress` (see C3).\n\n**B4. `software.apps[]` invents an apt package from the app's display name.**\n`:632-635`: `if name := (app.get(\"package\") or app.get(\"name\")): packages.append(name)` — unconditionally, even when the item only declares a `flatpak`/`snap`. `{\"name\": \"obs\", \"flatpak\": \"com.obsproject.Studio\"}` produced both `packages: [… obs …]` and a flathub runcmd (`max/user-data:328` + `:290`). `apps[].snap`, `apps[].appimage` and `apps[].preference` are never read at all. schema.md:420 also says unresolvable `apps` entries should *warn*, not abort — putting them in autoinstall `packages:` makes them abort.\n\n**B5. Flatpaks are installed from a remote that was never added.**\n`:841-842` emits `flatpak install -y flathub <app>` into cloud-init `runcmd`, and `:646` adds the `flatpak` package. Ubuntu's `flatpak` package ships no remotes; without `flatpak remote-add --if-not-exists flathub …` every one of these fails at first boot, and cloud-init `runcmd` failures are logged, not fatal. Every declared flatpak silently does not exist.\n\n**B6. Snaps are emitted twice, once as a broken command.**\n`:655-658` emits the native `snaps:` list. `lis_common.py:903-907` *also* emits, per snap, `apt-get install -y snapd …` and `f\"snap install {snap} || true\"` where `snap` is the dict. Verified output (`max/user-data:147`):\n`snap install {'name': 'lxd', 'channel': 'latest/stable', 'classic': False} || true` — a Python repr in a shell command, saved only by `|| true`.\n\n**B7. `desktop.display_manager: \"gdm\"` emits a package that does not exist on Ubuntu.**\n`lis_common.py:875` maps `gdm → gdm`; `:915` emits `apt-get install -y gdm` **without** `|| true`. `packages.ubuntu.com/noble/gdm` returns \"No such package\" (Ubuntu's package is `gdm3`, and no virtual package covers it). Verified in `max/user-data:153`. `translate_desktop()` (`:872`) accepts the spelling `gdm3`, which the LIS schema enum does not permit — so the only spelling a valid document can use is the one that breaks.\n\n**B8. `network.manager: networkmanager` installs NM but leaves netplan rendering to networkd.**\n`chroot_intents` emits `apt-get install -y network-manager` + `systemctl enable network-manager` (`max/user-data:165,171`), while `network_config()` (`:913`) emits a netplan block with no `renderer: NetworkManager`. Two stacks end up owning the same interface. Also: `network_config()` only checks `manager` in the *no-interfaces* branch (`:884-886`), where it *refuses* `systemd-networkd` — which `chroot_intents` happily enables. The applier refuses something it simultaneously implements.\n\n**B9. A declared keyfile unlock is silently converted into a boot-time passphrase prompt.**\n`:814-816` rewrites the third crypttab field to `none` for every LUKS container. There is a comment explaining why, but no `warn()`. A document that says \"unlock this container from keyfile X\" produces a system that interactively prompts on every boot — the opposite of the declared intent, and invisible to the operator.\n\n**B10. `registration.server` and `registration.email` are read and thrown away.**\n`lis_common.py:835`: `REGISTRATION[\"ubuntu\"] = \"pro attach $TOKEN\"`. `:859-861` formats `org=` and `email=` into a template that contains neither placeholder, so both are consumed (no tracker warning) and dropped. Verified: `max/user-data:129` is `TOKEN=$(cat …); pro attach $TOKEN`. Ubuntu Pro has `pro config set contract_url=…` for a non-default server. schema.md:§15 calls silently skipping a subscription \"drift\".\n\n**B11. Undeclared swap appears on every install.** See C1 — the applier never emits curtin's `swap: {size: 0}`, and curtin's default is to create `/swap.img` by heuristic when no swap device exists. Any document without swap gets swap.\n\n**B12. `files[]` land on first boot, not at install time.**\n`:846-854` routes them into cloud-init `write_files` under `autoinstall.user-data`. Every `scripts.post_install` late-command (`:749-755`) runs *before* that, so a post-install hook cannot read a file the same document declares. schema.md:§13 describes `files` as the Ignition primitive (present in the installed image), and `lis_common.file_commands()` — used by agama/kickstart/alpine/debian/arch — exists precisely to write them in-target. Not imported here.\n\n**B13. `storage.swap.file` on a btrfs root will fail the install.**\n`:544-547` emits `fallocate -l 2G /swapfile && … mkswap`, unquoted by `|| true`. btrfs swapfiles require NOCOW/no-compress creation; `mkswap`/`swapon` on a plain fallocated file fails. The e2e recipes never combine `swap.file` with btrfs.\n\n**B14. Stage conflation with no warning.** `scripts.pre` and `pre_install` both become `early-commands` (`:711-714`); `post_install`, `post`, `pre_reboot` and `on_success` all become undifferentiated `late-commands` (`:749-755`). schema.md:§13 defines `pre_reboot` as \"after unmounting, right before rebooting\" and `on_success` as conditional on zero errors — neither property survives, and neither is reported.\n\n---\n\n# C. UNUSED INSTALLER CAPABILITY — expressible in LIS today\n\n**C1. `storage: {swap: {filename, size}}` (curtin config item, reachable from autoinstall `storage:`).**\nThe autoinstall reference states the curtin `grub` and `swap` items \"can also be included here\". Curtin documents `filename` (default `/swap.img`), `size` (\"Setting `size` to 0 will disable swap\"), `maxsize`. `storage.swap.file{path,size}` maps 1:1; the applier uses a fallocate late-command instead (`:544`, with a `warn`). And no LIS document can currently *suppress* subiquity's default swapfile.\n\n**C2. `error-commands` — the applier refuses `scripts.on_error` claiming no such hook exists.**\n`:756-757`: `refuse(\"scripts.on_error has no autoinstall equivalent (no failure hook)\")`. The autoinstall JSON schema contains `error-commands` (array of string-or-argv), run when the install fails — exactly this. This is a wrong refusal blocking valid documents, and the one LIS field that maps to it verbatim.\n\n**C3. netplan `wifis:` and `match: {macaddress:}`.**\n`:888/:912` refuse `network.wifi` as \"not expressible in Ubuntu Server autoinstall\"; the autoinstall `network` schema explicitly lists `wifis` (with the same `match` sub-schema as `ethernets`). netplan `access-points[ssid].password` accepts a 64-hex PSK, matching `wifi[].psk_hash`, and `hidden: true` matches `wifi[].hidden`. Likewise `match.macaddress` covers `network.interfaces[].match.mac`, which the applier refuses at `:898`. Also unused: `bonds`, `bridges`, `vlans`, `tunnels`, `dhcp6`, `nameservers`.\n\n**C4. `kernel-crash-dumps: {enabled: bool}` ↔ `system.kdump`.** Native autoinstall key; `system.kdump` is currently unread.\n\n**C5. `source: {id: …}` ↔ `software.role`.** `ubuntu-server-minimal` vs `ubuntu-server` is the native expression of `role: minimal` (see A1). `source.search_drivers` is also unused.\n\n**C6. `storage: {grub: {probe_additional_os, terminal, install_devices, update_nvram}}`.**\nCurtin's grub item is reachable from autoinstall `storage:`. `boot.os_prober` → `probe_additional_os`; `boot.console.serial` → `terminal`. Both are currently only warned by `check_boot_extras` (`:1021`).\n\n**C7. `identity.groups`.** The comment at `:760-761` says the identity section \"carries no group or shell vocabulary\", so `users[0].groups` goes through a `usermod -aG` late-command (`:765-766`). The current autoinstall schema has `identity.groups` as either a list or `{override, append}`. The late-command works; the native key is simply unused, and the comment is stale.\n\n**C8. `apt.mirror-selection` / `apt.geoip` / `apt.disable_components` / `apt.sources` / `apt.preferences` / `apt.fallback`.**\n`:685` emits the legacy `apt: {primary: […]}` form. `mirror.country` (currently only warned by `check_mirror`) maps to `apt.mirror-selection.primary: [\"country-mirror\"]` plus `geoip: true`. `disable_components` matters here: `driver_packages` maps Ubuntu GPU/firmware names that live in `restricted`/`multiverse`, and nothing enables them.\n\n**C9. `partitions[].existing` is refused (`:202-205`) but partly expressible.** Curtin's `partition` action takes `number` + `preserve: true`, and `format`/`mount` actions can be omitted or given `preserve: true`, which is `existing: {match: {partition: N}, format: false}`. Only match-by-uuid/label/fs has no curtin equivalent.\n\n**C10. `proxy.https` / `proxy.no_proxy`.** Autoinstall `proxy` is a single URL, but the applier already writes arbitrary target files; schema.md:§14 requires the proxy to be \"persisted into the installed system's package-manager/environment configuration\". Both fields are currently unread.\n\n**C11. `system.time.*` ↔ cloud-init `ntp` module.** `auto[\"user-data\"]` is already used for `runcmd`/`write_files`; cloud-init's `ntp: {enabled, servers, ntp_client}` covers `system.time.ntp/servers/provider` exactly. All three are unread.\n\n**C12. `dm_crypt` `options`, `raid` `metadata`/`ptable`, `format` `uuid`/`extra_options`, `mount` `freq`/`passno`/`fstype`.** Curtin action keys the builder never emits. Only `format.extra_options` has no LIS field.\n\n---\n\n# D. SPEC GAPS — the installer can do it, the schema cannot say it\n\n- `codecs: {install: bool}` — restricted multimedia codecs. No LIS field; `drivers.*` is the nearest neighbour but is about hardware.\n- `updates: security|all` — whether to pull updates during install. No LIS field.\n- `refresh-installer: {update, channel}` — self-update the installer snap before running. No LIS field.\n- `debconf-selections` — a raw debconf preseed blob. Deliberately distro-specific; belongs in `x-ubuntu`, which the applier currently *refuses* (A11).\n- `reporting: {<name>: {type: print|rsyslog|webhook|none, endpoint}}` — install progress to a webhook. No LIS field; adjacent to `scripts.on_success`/`on_error` but is a native, structured channel.\n- `oem: {install: bool|\"auto\"}` — OEM/first-boot user mode. `docs/coverage-matrix.md` already lists this as `◌` deferred.\n- `active-directory: {admin-name, domain-name}` — domain join. `system.domain` exists but means DNS domain, not an AD realm; mapping it would be an invention.\n- `storage.layout: {name: lvm|direct|zfs, sizing-policy: scaled|all, password}` — the \"just give me a sensible layout\" shorthand. LIS is explicit-only; there is no way to say \"default layout, encrypted\".\n- `apt.preferences` (pinning) and `apt.sources` (extra repos) — schema.md:§11 explicitly pushes repositories to `x-*`, so this is a deliberate gap, not an oversight.\n- `zdevs` — s390x; moot, `check_arch(doc, {\"x86_64\"})` refuses non-x86 anyway.\n- `keyboard.toggle` — layout-switch key combo. `system.keymap` has console/layout/variant/font only.\n\n---\n\n## Two structural notes\n\n`check_unhandled(doc, ALL_SECTIONS)` at `:1020` is passed the complete section set and therefore cannot refuse any spec section — its only live effect is the incorrect `x-*` refusal. It provides none of the protection its docstring in `lis_common.py:93-108` describes.\n\n`seed_mount_commands` is imported at `:26` and never called (the applier inlines its own `blkid -L LIS` variant at `:697-700`). `file_commands`, `shell_packages`, `kernel_params_commands` and `check_user_sudo` are all *not* imported, though every peer applier uses at least some of them — A3 and B12 are the consequences.",
      "I verified everything against the bookworm udebs (fetched to `/tmp/dibits/x/…`) plus the existing clones at `/tmp/pauto`, `/tmp/pax` (partman-base), `/tmp/palvm`, `/tmp/pcrypto`, and `doc/devel/partman-auto-recipe.txt` (`/tmp/dibits/par.txt`). Test documents used: `/home/bresilla/lis/docs/examples/*` plus four I wrote in the scratchpad (`deb-max.lis.json`, `two.lis.json`, `rootkey.lis.json`, `sv.lis.json`).\n\n---\n\n# A. SILENT DROPS — declared intent, no warning, no refusal\n\n**A1. `storage.partitions[].disk` never reaches the recipe. Multi-disk LVM lands on the wrong disks.**\n`recipe_entry()` is called for PV partitions at `lis2debian.py:501-503` and emits `$primary{ } method{ lvm } vg_name{ vg0 }` — never `device{ … }`. partman documents `device{ /dev/hdb }` as the way to pin a PV to a disk (`partman-auto-recipe.txt:255-264`), and `partman-auto-lvm/lib/auto-lvm.sh:247-253` builds the main-device scheme from *\"PV entries with `device{ $main_pv }`\" plus \"PV entries without `device{`\"* — i.e. every device-less PV goes on disk 1. Lines `:259-264` then give each extra disk `add_envelope \"\"`, a synthesised `100 1000 -1 ext3 method{ lvm }` that eats the whole disk.\nReproduced with `/tmp/.../scratchpad/two.lis.json` (pv1 10 GiB on `a`, pv2 20 GiB on `b`): output is\n`… 10240 10240 10240 ext4 $primary{ } method{ lvm } vg_name{ vg0 } . 20480 20480 20480 ext4 $primary{ } method{ lvm } vg_name{ vg0 } .`\nBoth land on `/dev/vda` (30.5 GiB demanded from one disk), `/dev/vdb` gets an undeclared full-disk PV. **0 warnings, 0 refusals.** The `server-lvm-pool.lis.json` example is exactly this shape.\n\n**A2. `role: esp` never emits `method{ efi }` — a UEFI install stops in the partitioner and gets no `/boot/efi`.**\n`lis2debian.py:520-522` routes the ESP through the generic path, producing (verified in `dbg/max/preseed.cfg`):\n`512 512 512 fat32 $primary{ } $bootable{ } method{ format } format{ } use_filesystem{ } filesystem{ fat32 } mountpoint{ /boot/efi } .`\npartman-efi `check.d/07efi:33-40` counts a partition only if `method = efi` **and** `$id/bootable` exists; `choose_method/45efi/do_option:8-16` is the only thing that writes those, and it *deletes* `use_filesystem`/`filesystem`/`mountpoint`. `fstab.d/efi:36` emits the `/boot/efi vfat umask=0077` line solely for `method = efi`. Debian's own recipe is `768 788 1024 free … method{ efi } format{ } .` (`/tmp/pauto/partman-auto/recipes-amd64-efi/atomic:3-8`). Result on UEFI: `partman-efi/no_efi` critical dialog (never preseeded, nor is `partman-efi/non_efi_system`), and no ESP mount.\nThis path is **untested**: every `docs/examples/test-*.lis.json` is `firmware: bios` with `role: boot`; only `server-btrfs`/`server-lvm-pool`/`desktop-encrypted` use `esp`, and all three are refused for unrelated reasons before this would show.\n\n**A3. `root`'s `ssh_authorized_keys` and `dotfiles` are written to `/home/root`.**\n`lis2debian.py:934-936` hardcodes `/home/{user['name']}` for every user including root; same in `lis_common.py:951-954` (dotfiles) and `:743` (`chown -R … /home/{name}`). Verified with `rootkey.lis.json`:\n`in-target sh -c 'install -d -m700 -o root /home/root/.ssh && echo … >> /home/root/.ssh/authorized_keys'` — sshd reads `/root/.ssh/authorized_keys`. 0 warnings.\n\n**A4. Non-primary users' SSH keys and `scripts.post_install` are emitted before the account is created.**\nThe `for user in users:` loop (`:929-938`) runs before the `for user in normal[1:]:` useradd loop (`:939-949`). Verified ordering in `dbg/max/preseed.cfg`: `install -d -m700 -o bob …` and `in-target su - bob -c …` both precede `in-target useradd … bob`. `install(1)` fails on an unknown user, the `&&` short-circuits, and preseed-common `bin/preseed_command:17` does `log-output sh -c \"$command\" || code=$?` on the whole `;`-joined string — only the *last* command's status is checked, so every intermediate failure is swallowed with no dialog.\n\n**A5. `users[1:].comment` is dropped.** `useradd` at `:945-949` has no `-c`. Only the primary's comment is used (`:752-753`), and because the tracker collapses list indices that read marks `users[].comment` as consulted, so `check_unread` stays quiet.\n\n**A6. `system.time.*` never reaches the installed system, and `servers[1:]` are dropped.**\n`:721-723` emits `clock-setup/ntp` and `clock-setup/ntp-server`. clock-setup `postinst:29-51` uses `ntp-server` only to drive the installer's own `rdate`; `usr/lib/finish-install.d/10clock-setup` writes nothing about NTP into `/target` (it only handles UTC/localtime). Worse, `postinst:23-27` *overwrites* the preseeded value with the first DHCP-supplied server. So `system.time.servers` never configures the target, and `system.time.ntp: false` only skips the installer's clock sync — the `standard` task still pulls systemd-timesyncd.\n\n**A7. `network.ssh.enabled: false` does nothing, and silently contradicts `software.role: server`.**\n`:849` is `if ssh.get(\"enabled\"):` with no else branch, so `false` emits nothing while the `ssh-server` task (`:826`) installs and enables sshd. `password_auth` / `permit_root` are also only applied inside that branch (they *do* get an unread-warning in that case, but only because they are never read).\n\n**A8. The root btrfs subvolume's `mount_options` are dropped.**\n`subvol_script:628` only does `sed -i s/subvol=$current/subvol=@/` on partman's existing fstab line; only *nested* subvolumes get their options (`:631`). Reproduced with `sv.lis.json` (`@` declares `compress=zstd,noatime`): output is `sed -i s/subvol=\"$current\"/subvol=@/` and `printf … subvol=@home,compress=zstd` — `@`'s options vanish, 0 warnings. Tracker can't see it (collapsed path shared with the nested entries).\n*Verified-correct nearby:* the `@`/`@home` names are **not** mangled here, unlike lis2kickstart.\n\n---\n\n# B. DRIFT — emitted config that differs from what was declared\n\n**B1. `boot.os_prober` is written to a key that is never read, and the real knob is never touched.**\n`:782-783` emits `grub-installer/with_other_os boolean <os_prober>`. In grub-installer 1.194, `with_other_os`/`only_debian` is `$q`, asked only inside the `elif [ \"$state\" = 1 ]` branch of the loop at `usr/bin/grub-installer:866-905` — and the loop `break`s at `:869-875` whenever `grub-installer/bootdev` is *seen*, which the applier always makes true (`:787`). So the value has no effect at all. The actual os-prober switch is `grub-installer/enable_os_prober_otheros_yes` / `_no` (`:634-659`, defaults `true`/`false`), which set `grub2/enable_os_prober` in the target. Net: `os_prober: false` still enables os-prober when another OS is present; `os_prober: true` still leaves it off on a single-OS machine.\n\n**B2. `passwd/user-default-groups` *replaces* Debian's default group list.**\n`:764` emits `d-i passwd/user-default-groups string docker sudo`. user-setup-udeb `templates:14-16` gives this a `Default: audio cdrom dip floppy video plugdev netdev scanner bluetooth debian-tor lpadmin`, and `usr/bin/user-setup-apply:137-141` iterates `$RET` verbatim. A document saying `groups: [\"docker\"]` therefore *removes* audio/video/plugdev/netdev/cdrom/lpadmin from the primary account — memberships it never asked to drop. (Secondary users are fine: `useradd -G` is additive.)\n\n**B3. `mirror.url` loses its scheme, and `mirror.country` overrides it.**\n`:1008-1011` always writes `mirror/http/hostname` + `mirror/http/directory`. choose-mirror addresses its keys as `mirror/%s/%s` keyed on the protocol (`strings bin/choose-mirror`: `mirror/https/hostname`, `mirror/https/directory`, `mirror/https/proxy` all exist), and `mirror/protocol` (Default `http`) is never emitted — so `\"https://…\"` silently installs over plain http. Separately, `mirror/country`'s template is literally `country code or \"manual\" (for internal use)`, and the official example-preseed (`example-preseed.txt:105-108`) pairs an explicit hostname with `mirror/country string manual`. `:1006` emits the real country code, so the declared `mirror.url` is overridden by the country's mirror list.\n\n**B4. `system.telemetry: \"default\"` is emitted as an opt-out.**\n`:839-841` is `(\"true\" if telemetry == \"on\" else \"false\")`. `\"on\"` is not in the schema enum (`schema.json:649-654` is `off | default`), so both legal values produce `popularity-contest/participate boolean false`. schema.md:324-325 defines `default` as *not* obliging an opt-out.\n\n**B5. `installer.on_finish` defaults to `reboot`; the spec default is `stay`.**\n`:1015` is `installer.get(\"on_finish\", \"reboot\")`. schema.md:550: \"`on_finish`: `reboot` | `poweroff` | `stay` (default `stay`)\". A document that omits the field gets an automatic reboot from an applier that would have *refused* `stay` if it were written down.\n\n**B6. `software.role: minimal` is indistinguishable from an absent role.**\n`:823-831`: both map to `tasksel/first multiselect standard`. `standard` is a real task (~50 packages); the spec treats `minimal` as a distinct coarse intent that appliers \"MUST fail on … they cannot provide\" (schema.md:415-418). The dead `desktop:mate` / `desktop:cinnamon` entries in that table can never be reached — the schema pattern (`schema.json:903`) forbids them.\n\n**B7. Script lifecycle stages are collapsed and re-homed without a warning.**\n`:925-928` funnels `post_storage`, `post_install`, `post`, `pre_reboot`, `on_success` into one `preseed/late_command` as `in-target sh -c …`. Per schema.md:489-493, `post_storage`, `pre_reboot` and `on_success` all run **in the live installer environment**, and `post_storage` runs right after formatting/mounting. late_command runs from `finish-install.d/07preseed`, inside the target chroot, at the very end. `check_script_fields` only warns when `chroot` is *explicitly* set against `chroots_by_default=True` (`lis_common.py:1111-1117`), so a document that omits `chroot` gets no warning. `on_success` is also emitted before the `files[]`/firstboot/birth-certificate commands, so it fires even when those later fail. Likewise `scripts.pre` and `scripts.pre_install` are merged into one `preseed/early_command` (`:859-860, :905-906`).\n\n**B8. Invented defaults when the document is silent.** `debian-installer/locale en_US.UTF-8` (`:707`), `keyboard-configuration/xkb-keymap select us` (`:710, :714`), `time/zone string UTC` (`:719`), `clock-setup/ntp boolean true` (`:721`). Defensible for unattended runs, but they are answers the document did not give. Related: when only `keymap.console` is declared it is fed straight to `xkb-keymap` (`:710`) — console keymap names and xkb layout names are different namespaces (`de-latin1` vs `de`, `uk` vs `gb`), and the warning at `:711-713` only fires when *both* are present.\n\n---\n\n# C. UNUSED INSTALLER CAPABILITY — expressible in LIS today, never emitted\n\nAll of these currently produce only a `check_unread`/`check_*` warning (or a refusal), never output.\n\n| LIS field | d-i vocabulary that exists | Evidence |\n|---|---|---|\n| `storage.partitions[].mount_options`, `lvm[].volumes[].mount_options` | `options/<opt>{ <opt> }` in the expert_recipe | `partman-auto-recipe.txt:200-203`; partman-base `lib/recipes.sh:337-355` writes any `foo{ }` into `$id/foo` (incl. `a/b` paths); `partman-basicfilesystems/bin/get_mountoptions:27-31` reads `$part/options/$op`. Allowlists per fs: ext4 `discard noatime nodiratime relatime nodev nosuid noexec ro sync usrquota grpquota user_xattr`; btrfs adds `compress ssd discard nodatacow …` |\n| `storage.partitions[].label`, `lvm[].volumes[].label` | `label{ fred }` | `partman-auto-recipe.txt:205-211`; partman-btrfs `commit.d/50format_btrfs:54-62` → `mkfs.btrfs -L`; partman-ext3 `commit.d/50format_ext3:67-68`; partman-target `check.d/05duplicate_labels` |\n| `boot.password_hash` | `grub-installer/password-crypted` | grub-installer `templates:2456`; example-preseed. schema.md:282-284 says an applier whose loader lacks the concept **MUST fail** — GRUB has it, so warning-and-drop is the worst of both |\n| `boot.kernel.variant: realtime` (and `cloud`) | `base-installer/kernel/image` | example-preseed; `linux-image-rt-amd64` exists in bookworm (verified in the archive `Packages`). `:790-792` refuses every non-default variant |\n| `network.wifi[]` (ssid + psk, hidden) | `netcfg/wireless_essid`, `netcfg/wireless_security_type` (`Choices-C: wep/open, wpa`), `netcfg/wireless_wpa` | netcfg `templates:634, 1082-1086, 2234`; `finish-install.d/55netcfg-copy-config:64-70` copies the wireless config into the target. `:730-731` says \"not expressible in a preseed\" — that is inaccurate |\n| `network.interfaces[]` (static, single NIC) | `netcfg/disable_autoconfig`, `get_ipaddress`, `get_netmask`, `get_gateway`, `get_nameservers`, `netcfg/confirm_static` | example-preseed; netcfg `templates:4538, 9900`. `:735-737` refuses outright |\n| `network.interfaces[].match.name` | `netcfg/choose_interface` | example-preseed. Not emitting it means a multi-NIC machine **prompts** mid-install |\n| `network.hosts[]` | trivially a late_command `/etc/hosts` append (the applier already does exactly this for the hostname at `:773-776`) | — |\n| `system.extra_locales` | `localechooser/supported-locales multiselect` | localechooser `templates:3033-3037`. Currently done via a `locale-gen` late_command (`lis_common.py:626-634`) — works, but not the native path |\n| `system.keymap.font` | console-setup `console-setup/fontface47`, `fontsize` | console-setup-udeb ships the ekmap/font udebs; only `keyboard-configuration/*` is emitted (`:714-716`) |\n| `boot.console.serial` | `debian-installer/add-kernel-opts` (already used at `:789`) + `finish-install/keep-consoles` | example-preseed |\n| `proxy.https` | `mirror/https/proxy` (with `mirror/protocol https`) | apt-mirror-setup `generators/50mirror:251-255` persists `Acquire::$protocol::Proxy` into the target — so `proxy.http` **is** honored end-to-end; `https` and `no_proxy` are not |\n| `installer.answers` | raw debconf lines are the native d-i answer format; this is a passthrough | schema.md:557 |\n| `installer.interactive` / `unattended` | `debconf/priority`, `preseed/interactive` | — |\n| `users[].uid` (primary) | `passwd/user-uid` | example-preseed. Currently a post-hoc `usermod -u` (`lis_common.py:742`) |\n| `storage.encryption[].type: luks1` | *no* partman-crypto vocabulary — `lib/crypto-base.sh:358-361` calls `cryptsetup luksFormat` with no `--type`, so the cryptsetup default (LUKS2) always wins | This one should be a **refusal**, not the current tracker warning: `luks1` is an on-disk-format request that silently comes out as LUKS2 |\n\nAlso worth flagging: `apt-setup/contrib`/`non-free`/`non-free-firmware` are emitted **only** when `drivers.*` produced a package (`:798-803`). A document that names a non-free package in `software.packages` gets no components enabled; pkgsel `postinst:169-177` calls `aptfailed`, which `exit $ret`s — a hard install failure rather than a silent one, but avoidable.\n\n---\n\n# D. SPEC GAPS — d-i can do it, the schema cannot say it\n\n- **Extra apt repositories / security & updates sources.** `apt-setup/services-select` (`Choices-C: security, updates, backports`, apt-setup-udeb `templates:1370-1373`), `apt-setup/local0/{repository,comment,key,source}`, `apt-setup/security_host`, `apt-setup/use_mirror`, `apt-setup/multiarch`. schema.md:437-438 explicitly pushes repositories into `x-*` extensions, so this is a deliberate gap — but it means the applier cannot express \"no backports, security only\".\n- **Partition table type.** `partman-partitioning/default_label` (gpt/msdos) and `$iflabel{ }` / `method{ biosgrub }` (`recipes/server:4-6`). LIS has `target.firmware` but no way to say \"GPT on a BIOS box\", which is exactly when a `biosgrub` partition is mandatory. `role: raw` cannot express it (it emits `method{ keep }`).\n- **fstab device naming.** `partman/mount_style` (`traditional|uuid|label`, partman-target `finish.d/40fstab_hd_entries:44-52`). No LIS field.\n- **LUKS cipher parameters.** `partman-crypto/{cipher,keysize,ivalgorithm,keyhash}` (`/tmp/pcrypto/partman-crypto/debian/partman-crypto.templates:63,75,90,122`), plus OPAL self-encrypting-drive support (`crypto-base.sh:320`, `partman-crypto/nested_opal`). `storage.encryption` has only `type` and `unlock`.\n- **Package-set policy.** `pkgsel/upgrade` (none/safe-upgrade/full-upgrade), `pkgsel/run_tasksel`, `base-installer/install-recommends`, `base-installer/kernel/image`. `software.role` is coarser than any of these; `install-recommends: false` in particular is the standard way to get a genuinely minimal Debian and there is no LIS spelling for it.\n- **EFI fallback bootloader.** `grub-installer/force-efi-extra-removable`, `grub-installer/update-nvram` (grub-installer `templates:4951, 5314`). `boot.secure_boot` / `boot.uki` are the nearest fields and neither means this.\n- **Installer-side remote access.** `network-console/{password,authorized_keys_url}`, `anna/choose_modules`. `installer.*` has no vocabulary for the installer's own network console.\n- **Target network config style.** `netcfg/target_network_config` (`nm_config|ifupdown|loopback`, `55netcfg-copy-config:33-49`). `network.manager` is close but not the same question. (No bug today: late_command runs at finish-install `07` and installs network-manager before `55`, so auto-detection picks `nm_config` correctly.)\n\n---\n\n## Verified-correct (checked and found sound, listed so they are not re-audited)\n\n- The `partman-crypto check.d/11unsafe_swap` refusal (`:328-336`), the encrypted-`/boot` and encrypted-root-without-`/boot` refusals (`:305-315`), and the `partman-basicfilesystems/no_swap` guard (`:566-575`) all match the udeb sources.\n- `auto-lvm.sh:186-190` really does `bail_out unusable_recipe` on a recipe with no `$lvmok`, which is why the bare-LUKS path uses `method=regular` + the `display.d` hook — the long comment at `:369-383` is accurate.\n- The RAID \"one recipe per disk\" refusal (`:416-423`) is correct: `partman-auto/display.d/initial_auto:80-90` passes every id to `autopartition`, which loops `perform_recipe` per device with the same recipe. (`device{ }` does *not* help there — it is only honored by auto-lvm.)\n- `subvol_script` reading the root subvolume name back out of the generated fstab is right: partman-btrfs `fstab.d/btrfs:26` hardcodes `subvol=@rootfs`, and `40fstab_hd_entries:38-42` sends `/dev/mapper/*` through `mapdevfs` rather than emitting a UUID — both comments at `:650-665` check out.\n- `tasksel/first multiselect standard, gnome-desktop` is fine without the parent task: `task-gnome-desktop` `Depends: … task-desktop` (bookworm `Packages`).\n- `proxy.http` is genuinely persisted into the installed system (apt-mirror-setup `50mirror:251-255`).\n- The generated-line check at `:1053-1057` and the \"encryption declared but no `method{ crypto }`\" check at `:1064-1069` are good fail-closed guards.",
      "## Environment note\n\nAll installer citations below are read from the Fedora 41 `install.img` tree already extracted at `/tmp/f41root` (only `pykickstart`, `pyanaconda`, `blivet` are present — no comps/`.treeinfo`, so group names could not be verified). Kickstart output was re-parsed with the real F41 handler (`pykickstart.version.makeVersion(\"F41\")`) to check what Anaconda actually keeps. Probe documents: `/tmp/nix-shell.ouGjCL/claude-1000/-home-bresilla-lis/67872401-f199-4cc3-ab7b-7d06474ee4e8/scratchpad/ks-audit-{max,b,c,d,e}.lis.json`.\n\n---\n\n# A. SILENT DROPS\n\n## A1. RAID under LUKS under LVM — the whole encryption layer vanishes and the array is mounted at `/`\n**Highest severity.** Probe `ks-audit-d.lis.json` (`raid md0` → `encryption cryptmd over md0` → `lvm vg0 devices:[cryptmd]`) produces, with **zero refusals and one unrelated warning**:\n\n```\nraid / --level=RAID1 --device=md0 raid.r1 raid.r2\nvolgroup vg0 pv.cryptmd            # pv.cryptmd is never declared anywhere\n```\n\nThree independent defects compose:\n- `lis2kickstart.py:158` — `encryption.get(handle)` is consulted **only inside the partitions loop**. The `raid` loop (`:204-214`) never looks at `storage.encryption`, so `--encrypted` / `--passphrase` are never emitted for an array. `DEFERRED_CRYPT` stays empty; no `%pre`, no seed read.\n- `lis2kickstart.py:210-212` — `in_lvm = any(array[\"name\"] in g.get(\"devices\", []))` compares raw handles. The VG names the *container* (`cryptmd`), not the array, so this is False and `target` falls back to the hardcoded `\"/\"` (`:212`). The array gets mounted as root with the default fstype.\n- `lis2kickstart.py:217` — `pvs = \" \".join(f\"pv.{d}\" ...)` uses the raw handle instead of `backing()` (`:99-106`), emitting a dangling `pv.cryptmd`.\n\nThe tracker is silent because `storage.encryption[].id/over/key/unlock` are all read by `luks_key_path()`/`enrollment_commands()` — read, then never emitted. `check_raid_consumers()` (`lis_common.py:646`) accepts encryption as a consumer, so nothing refuses either. Anaconda would fail at `volgroup` (\"Tried to use undefined partition\") *after* having scheduled the array as an unencrypted `/`.\n\n## A2. `software.services.enable` is silently discarded whenever `network.ssh.enabled` is true\n`lis2kickstart.py:381-390` emits two `services` lines:\n```\nservices --enabled=chronyd --disabled=cups\nservices --enabled=sshd\n```\nRe-parsed with the F41 handler: `services.enabled = ['sshd']`, `services.disabled = ['cups']`. `chronyd` is gone. Cause: `FC6_Services.parse` calls `set_to_self` (`pykickstart/commands/services.py:71-77`), and `set_to_obj` (`pykickstart/base.py:175-184`) assigns only non-`None` values — the second `services` overwrites `enabled` wholesale. Every unit in `software.services.enable` is dropped unless `sshd` happens to already be listed. Emitted, parsed away, no warning.\n\n## A3. `boot.kernel.params` disappear when the document declares no `storage`\n`render_storage()` returns at `lis2kickstart.py:63-64` if `storage` is empty, so no `bootloader` line is ever emitted; `:372-376` then scans for a line starting with `\"bootloader \"`, finds none, and drops the params. Probe `ks-audit-c.lis.json` (`params: [\"quiet\",\"audit=0\"]`, no storage) produces **zero warnings, zero refusals** and a kickstart with no `bootloader` command at all. The tracker is silent because `boot.kernel.params` *is* read at `:372`.\n\n## A4. `network.ssh.permit_root` is inverted or dropped\n`lis2kickstart.py:333` maps both `\"yes\"` **and** `\"prohibit-password\"` to `rootpw --allow-ssh`, and `\"no\"` to nothing.\n- `--allow-ssh` writes `PermitRootLogin **yes**` (`pyanaconda/modules/users/installation.py:163,186-190`). A document declaring `prohibit-password` gets password-based root SSH login enabled. That is a security-relevant inversion, not an approximation.\n- Omitting `--allow-ssh` writes nothing at all (`installation.py:192-194` — \"Not adding an override\"), leaving OpenSSH's `prohibit-password` default. So `permit_root: \"no\"` leaves root key-login enabled. Read at `:333`, so no tracker warning.\n\n## A5. Every field of a `users[]` entry named `root` except `password.hash` is dropped\n`lis2kickstart.py:338-339` `continue`s on root before the `sshkey`, `--shell`, `--groups`, `--gecos` emission. Probe `ks-audit-b.lis.json` declares `root.ssh_authorized_keys`, `root.shell: /bin/zsh`, `root.comment`, `root.groups: [adm]`; the output contains only `rootpw --iscrypted $6$r$r` and one `sshkey --username=bob`. `sshkey --username=root` is perfectly legal kickstart (`pykickstart/commands/sshkey.py`, `F22_SshKey`). No warning fires because `declared_paths()` collapses list indices — bob's keys mark `users[].ssh_authorized_keys` as read. `zsh` even gets installed by `shell_packages()`, making the drop look honored.\n\n## A6. `proxy` is emitted as a comment while the warning claims it was applied\n`lis2kickstart.py:512-514` inserts `# proxy: http://proxy:3128` and warns *\"proxy.http is applied to the installer environment only\"*. Nothing is applied anywhere: a `#` line is inert. Schema.md §14 says proxy \"Applies to the installation *and* is persisted into the installed system's package-manager/environment configuration.\" Both halves are dropped, and the warning actively misreports it. (`proxy.https` / `proxy.no_proxy` do at least get honest unread warnings.)\n\n## A7. `registration.email` is read and thrown away\n`lis_common.py:859-861` builds `email=f\" -e {registration['email']}\"` and passes it to `template.format(...)`, but the `\"fedora\"` template (`lis_common.py:833`) has no `{email}` placeholder. Confirmed in `ks-audit-max` output: `subscription-manager register --org=myorg --activationkey=$TOKEN` — no email. Reading it suppresses the tracker warning.\n\n## A8. `storage.partitions[].label` is dropped on any btrfs partition carrying subvolumes\n`lis2kickstart.py:185-192` rebuilds `flags` from scratch, deliberately preserving only encryption flags, and `--label` is lost. Confirmed by re-parse of `ks-audit-max`: the document declares `label: \"ROOT\"`; the parsed `PartData` for `btrfs.rootp` has `label=''`. The btrfs volume is then labelled with the **handle** (`--label=rootp`, `:196`), a value the document never wrote. `storage.partitions[].label` is read for other partitions, so no warning.\n\n---\n\n# B. DRIFT\n\n## B1. `role_mountpoint()` synthesises a second `part /boot`, and Anaconda destroys the first\n`lis2kickstart.py:141` fills a missing mountpoint from `ROLE_MOUNT`. `docs/examples/test-raid.lis.json` declares a mirror partition `boot2` on the second disk with `role: boot` and **no** mountpoint; the applier emits:\n```\npart /boot --fstype=ext4 --size=1024 --ondisk=vda\npart /boot --fstype=ext4 --size=1024 --ondisk=vdb\n```\nAnaconda's handler (`pyanaconda/modules/storage/partitioning/custom/custom_partitioning.py:370-376`): *\"If a previous device has claimed this mount point, delete the old one\"* → `storage.destroy_device(device)`. The `/boot` on `vda` is cancelled; `/boot` lands on `vdb`. This is in a **passing e2e recipe**, so the drift is invisible to the suite.\n\n## B2. `network.firewall.enabled: false` produces `firewall --disabled` *and* installs+enables firewalld\nProbe `ks-audit-b`:\n```\nfirewall --disabled\n...\n%post: dnf install -y firewalld ; systemctl enable firewalld || true\n```\n`lis_common.py:930-942` gates on `if firewall := network.get(\"firewall\")` — truthiness of the dict, never `enabled`. `%post` scripts run **after** the firewall configuration task (`pyanaconda/installation.py:158-162` appends the firewall task to `os_config`; `:284-294` appends `post_scripts` later in the same queue), so `%post` wins and the target boots with firewalld enabled against an explicit `enabled: false`.\n\n## B3. `desktop.autologin` always writes GDM config regardless of `display_manager`\n`lis2kickstart.py:476-479` unconditionally writes `/etc/gdm/custom.conf`. Probe `ks-audit-b` declares `display_manager: \"sddm\"` and `autologin: \"bob\"`; the output installs sddm and writes a GDM file. Autologin never happens.\n\n## B4. Subvolume names are mangled: `@` → `root`, `@home` → `home`\n`lis2kickstart.py:201` and `:271`: `sub[\"name\"].lstrip(\"@\") or \"root\"`. Confirmed in output (`btrfs /home --subvol --name=home`) and by re-parse. Nothing in the stack requires this: `_execute_btrfs_data` passes `btrfs_data.name` straight through (`custom_partitioning.py:1155-1156, 1196`), and `BTRFSSubVolumeDevice._create` calls `blockdev.btrfs.create_subvolume(mountpoint, self.name)` with no validation (`blivet/devices/btrfs.py:628-634`). `@`/`@home` are valid btrfs names. Note `lstrip` also strips *all* leading `@`, and a subvolume literally named `@` becomes `root`.\n\n## B5. `software.apps[].preference` is ignored and both runtimes are installed\n`lis2kickstart.py:422-427`: an app object with both `package` and `flatpak` appends to `pkgs` **and** `flatpaks`. `ks-audit-max` installs `firefox` as an RPM *and* runs `flatpak install ... org.mozilla.firefox`. `preference` gets only an unread warning.\n\n## B6. `flatpak install ... flathub` targets a remote that is never added, under `%post --erroronfail`\n`lis2kickstart.py:474` emits `flatpak install -y --noninteractive flathub <app>` with no preceding `flatpak remote-add`. Fedora ships the `fedora` remote, not `flathub`. Under `%post --erroronfail` (`:487`) a non-zero exit aborts the installation. I did not test this on a live Fedora image — flagging as a likely runtime failure, not a verified one.\n\n## B7. `x-*` extension sections are refused, contrary to SPEC §2.2\n`lis_common.py:93-108` refuses any top-level key not in `ALL_SECTIONS`. Schema.md:43-44 — \"Keys beginning with `x-` are **extensions** and MUST be ignored by appliers that do not recognize them.\" `docs/examples/server-btrfs.lis.json` is refused by the Fedora applier solely for carrying `x-nixos`.\n\n## B8. Locked accounts get a double `!`\n`password_field()` (`lis_common.py:546-558`) returns `!<hash>`, emitted as `user --password=!$6$… --iscrypted --lock` (`lis2kickstart.py:354-358`). Anaconda then does `password = \"!\" + password` (`pyanaconda/core/users.py:502-503`), so the shadow field becomes `!!$6$…`. Correctly locked, but a later `passwd -u` leaves a stray `!` and the account still locked.\n\n## B9. Minor\n- `desktop.display_manager` is installed by `chroot_intents` (`lis_common.py:910-916`) yet `check_section_fields(doc, \"desktop\", {...})` (`lis2kickstart.py:556`) warns \"not applied by this applier\" — a false warning on every desktop document, which trains readers to ignore the channel.\n- `installer.on_finish: \"stay\"` emits `reboot` before refusing (`:392-396`); under `--lenient` the target reboots against an explicit `stay`.\n- `desktop.display_manager: \"none\"` is *refused* (`lis_common.py:911-913`) although it means \"install no DM\" — kickstart's `skipx` says exactly that.\n- `uid_commands` emits `usermod -u 0 root` and `chown -R 0 /home/root` for a root entry with `uid: 0`.\n- `DEFERRED_CRYPT` (`:54`) and `LV_BTRFS` (`:57`) are module-level and never cleared; a second in-process `render_kickstart()` would leak btrfs lines and `%pre` passphrase reads across documents. Latent only — `tools/e2e/installer.py:305` invokes the applier as a subprocess.\n\n---\n\n# C. UNUSED INSTALLER CAPABILITY (expressible in LIS today)\n\nEvery option below exists in the F41 command map (`pykickstart/handlers/f41.py`).\n\n1. **`%onerror` — `scripts.on_error` is refused for no reason.** `lis2kickstart.py:510-511` refuses it. `OnErrorScriptSection` is registered by Anaconda's real parser (`pyanaconda/kickstart.py:279`) and run by `runOnErrorScripts` (`pyanaconda/core/util.py:662-667`). Direct 1:1 mapping.\n2. **`%pre-install` — `scripts.post_storage` is downgraded to `%post` on a false premise.** The comment at `lis2kickstart.py:455` says \"Anaconda has no post-partition hook\". `pyanaconda/installation.py:373-383`: *\"Run %pre-install scripts with the filesystem mounted and no packages\"* — which is verbatim the LIS `post_storage` contract (schema.md:489).\n3. **`part/logvol/raid --fsoptions=`** (`partition.py:480`, `logvol.py:501`) — `storage.partitions[].mount_options` and `storage.lvm[].volumes[].mount_options` are currently only warned. (Subvolume `mount_options` genuinely have no `btrfs` option — that one is a real installer gap.)\n4. **`bootloader --password=<hash> --iscrypted`** (`bootloader.py:131,278`) — `boot.password_hash`. Schema.md:283 says an applier whose loader lacks the concept MUST fail; GRUB has it, so this should be emitted, not warned.\n5. **`bootloader --timeout=`** (`bootloader.py:231`) — `boot.timeout` is instead applied by `sed`-ing `/etc/default/grub` plus `grub2-mkconfig` in `%post` (`lis_common.py:232-235`). Works, but the native option is one flag.\n6. **`network --essid= --wpakey=`** (`network.py:456,592`) — `network.wifi` is *refused* at `lis2kickstart.py:316-317`. `psk_hash` is the 64-hex PSK, which NetworkManager accepts as `--wpakey`.\n7. **`network --device= --bootproto=static --ip= --netmask= --gateway= --nameserver= --noipv6`** (`network.py:429,473,476`) — the entire `network.interfaces[]` array is unread; every document gets `--bootproto=dhcp`. A static-IP document installs onto DHCP with only a warning.\n8. **`part --onpart=LABEL=x|UUID=x --noformat --resize --size=`** (`partition.py:417,428,605`), and the `mount` command (`mount.py:94-145`, explicitly designed to \"not require the whole storage stack to be described\") — `storage.partitions[].existing` is refused wholesale at `lis2kickstart.py:124-127` as \"not expressible in an unattended kickstart\". It is. The same applies to the `storage.wipe: false` refusal at `:91-93` (`clearpart --none` + `--onpart`).\n9. **`logvol --percent=`** (`logvol.py:446`) — `size_mib()` (`:44`) refuses percent sizes for LVM volumes. Percent for *partitions* is correctly refused; percent for logical volumes is native.\n10. **`logvol --label=`** (`logvol.py:655`) — `storage.lvm[].volumes[].label` is unread (warned).\n11. **`part/logvol/raid --luks-version=luks1|luks2`** (`logvol.py:869`) — `storage.encryption[].type` is unread on every encrypted document, including the passing e2e recipes.\n12. **`timesource --ntp-server= / --ntp-pool= / --ntp-disable`** (`timesource.py:100-112`) — `system.time.ntp` and `system.time.servers` are unread. (`timezone --ntpservers/--nontp` still parse but are deprecated at F40, `timezone.py:461-465`; `--utc`, which the applier uses, is **not** deprecated — that emission is correct.)\n13. **`url --proxy=` / `repo --proxy=`** (`url.py:93`, `repo.py:368`) — the installer half of `proxy.http` (see A6).\n14. **`%pre/%post --interpreter=`** (`sections.py:171`) and **`%post --nochroot`** (`sections.py:379`) — `scripts[].interpreter` and `scripts[].chroot: false` are warned as \"not applied\" by `check_script_fields`, but both are native section options.\n15. **`skipx`** — `desktop.display_manager: \"none\"`.\n16. **`%addon com_redhat_kdump`** for `system.kdump`. I could **not** verify that the kdump addon ships in the F41 `install.img` — the extraction at `/tmp/f41root` contains only the three Python packages. Treat as unconfirmed.\n\n---\n\n# D. SPEC GAPS (installer can do it, the schema cannot say it)\n\n- **Additional repositories.** `repo --name= --baseurl=/--metalink= --includepkgs= --excludepkgs= --cost= --install` (`repo.py`) has no LIS vocabulary — schema.md:518 explicitly pushes repo lists to `x-*`. This is why `driver_packages` must *refuse* `drivers.gpu: nvidia` on Fedora (`lis_common.py:147,178`): RPM Fusion is one `repo` line away, and the schema has no way to ask for it.\n- **DNF modules.** `module --name= --stream=` (`module.F41_Module`) — no LIS field.\n- **Identity backend.** `authselect` (`authselect.F28_Authselect`) and `realm join` (`realm.F19_Realm`) — LIS has no auth/domain-join section; `system.domain` is only a DNS suffix.\n- **Partition-table type.** `clearpart --disklabel=gpt|msdos` — LIS has no way to pin the label; the applier picks whatever Anaconda defaults to and then has to invent a `biosboot` partition (`:86-90`) to compensate.\n- **Installer-time remote access.** `sshpw --username= --password=` (`sshpw.F24_SshPw`) — no LIS equivalent.\n- **Payload knobs.** `%packages --ignoremissing / --excludeWeakdeps / --nocore / --inst-langs`, `--optional` on groups (`sections.py:605-755`) — no LIS field. Note schema.md:433 says an applier that cannot resolve a package name MUST fail, which is the opposite of `--ignoremissing`, so this may be deliberate.\n- **`user --homedir= --gid=`** (`user.py`) — LIS has `uid` but no home directory or primary GID.\n- **Subvolume mount options** — a genuine two-sided gap: LIS declares them, and the `btrfs` command has no `--fsoptions` (only `--data --label --metadata --mkfsoptions --name --noformat --parent --subvol --useexisting`). Blivet propagates the *volume's* mountopts to subvolumes (`blivet/devices/btrfs.py:568-583`), so per-subvolume options are not reachable through kickstart at all.\n- **`system.keymap.font`** — `keyboard` has only `--vckeymap --xlayouts --switch`. Correctly warned; no installer path short of `%post` writing `/etc/vconsole.conf`.\n\n## Unverified\nI could not check Fedora 41 comps, so `@server-product`, `@gnome-desktop`, `@kde-desktop`, `@xfce-desktop` (`lis2kickstart.py:405-411`) are unvalidated group ids, as is the refusal of `desktop:sway` / `desktop:hyprland` (`:413`) — Fedora ships a Sway spin, so that refusal may be unnecessary. The `@cinnamon-desktop` / `@mate-desktop` / `@budgie-desktop` entries in `role_groups` are dead code: `software.role`'s schema pattern (`schema.json:903`) does not permit those values.",
      "## A. SILENT DROPS — declared, neither emitted nor reported\n\nAll verified by running the applier and reading the two generated files. In each case the field *is* read somewhere, so `check_unread()` stays quiet, and no `warn()`/`refuse()` covers it.\n\n**A1. The whole `storage.encryption` section vanishes from the Agama profile.**\n`agama_drives()` (`tools/appliers/lis2agama.py:286-342`) consults `storage.encryption` only to work out which partitions LVM consumes (`:292-293`). `check_unsupported()` warns that RAID (`:59`) and LVM (`:295`) are AutoYaST-only — it says nothing about encryption. Verified: `docs/examples/test-encrypted.lis.json` produces a `profile.json` whose `/dev/vda3` is a plain btrfs root with subvolumes and no LUKS anywhere; the run emits exactly one warning (\"no x-suse.product declared\"). Agama expresses this natively as `storage.drives[].partitions[].encryption.luks2 {password, cipher, keySize, pbkdFunction}` (agama-project.github.io/docs/user/reference/profile/storage). Anyone taking the `--apply` path with an encrypted document gets an unencrypted disk, silently.\n\n**A2. `boot.loader` is never emitted by either renderer.**\n`check_boot_extras(doc, {\"loader\", …})` at `:671` lists `loader` as handled, and the only consumer is `boot_timeout_commands()` (`lis_common.py:211-235`), which uses it to pick *where to write the timeout*, not what to install. Verified with `boot.loader: \"systemd-boot\"`: zero warnings, zero refusals, `<bootloader>` contains only `<global><timeout>`, GRUB gets installed, and the chroot script writes a `timeout` line into `/boot/loader/loader.conf` that nothing reads. AutoYaST has `<bootloader><loader_type>` and documents a systemd-boot variant (doc.opensuse.org/projects/autoyast, bootloader section).\n\n**A3. `system.keymap.variant` is dropped in the profile that actually boots.**\n`render_autoyast()` `:392-395` builds the keymap from `console or layout` only. The variant is read at `:220` in `render_agama()`, which satisfies the tracker, and `check_keymap(doc, {\"console\",\"layout\",\"variant\"})` at `:675` declares it used. Verified: `{\"layout\":\"cz\",\"variant\":\"qwerty\"}` → `profile.json` `\"keyboard\": \"cz(qwerty)\"`, `autoyast.xml` `<keymap>cz</keymap>`. Exactly the two-format trap.\n\n**A4. Btrfs subvolume mountpoints are dropped in AutoYaST.**\n`:473-478` emits `<subvolume><path>…</path></subvolume>` and nothing else; `sub[\"mountpoint\"]` is read only on the Agama path (`:335`). AutoYaST derives the mount from the subvolume path, so a declared `{\"name\": \"data\", \"mountpoint\": \"/var/lib/data\"}` is verified to produce `<path>data</path>` — mounted at `<btrfs-root>/data`, not `/var/lib/data`. The Agama profile for the same document gets `\"mountPath\": \"/var/lib/data\"` right. Silent divergence, and silently wrong on the booting side.\n\n**A5. SSH keys.** `render_agama()` `:251-252` takes `root[\"ssh_authorized_keys\"][0]` and nothing else; `render_autoyast()` emits no keys at all. Because root's read marks the flat path `users.ssh_authorized_keys` as consulted, every other user's keys and root's second-through-nth keys disappear with no message. Verified with three keys across two users → one key in `profile.json`, none in `autoyast.xml`. AutoYaST's users resource has `<authorized_keys>` (doc.opensuse.org, users resource).\n\n**A6. `registration.server` is read and thrown away.** `lis_common.py:859` passes it as `org=`, but `REGISTRATION[\"suse\"]` (`:835`) is `\"SUSEConnect -r $TOKEN{email}\"` — no `{org}` placeholder. The read suppresses the tracker warning. Both targets can express it: Agama `product.registrationUrl`, AutoYaST `<suse_register><reg_server>`.\n\n**A7. Three script stages run at the wrong time, unannounced.** `collect_scripts()` `:108-110` concatenates `post_storage`, `post_install`, `post`, `pre_reboot`, `on_success` into one list that becomes `<chroot-scripts>` / Agama `scripts.chroot`. Per `spec/schema.md` §13, `post_storage` runs in the live installer right after partitioning, `pre_reboot` after unmount, `on_success` only on a clean finish. All three run inside the target chroot mid-install, and `on_success` runs unconditionally. `content` is read, so nothing reports it. Both installers have the right home for at least `post_storage`: AutoYaST `postpartitioning-scripts`, Agama `scripts.postPartitioning`.\n\n## B. DRIFT\n\n**B1 (BLOCKER). The applier refuses its own extension.** `check_unhandled(doc, ALL_SECTIONS)` at `:670` refuses any top-level key not in `ALL_SECTIONS`, which contains no `x-*` pattern. `x-suse.product` is read at `:226` and `:484`, and `:487` actively tells the operator to set it — but a document that does exits 1: `refused: section 'x-suse' is declared but this applier does not translate it`. Verified. `spec/schema.md` §2.2/§18 requires `x-*` to be ignored, let alone honored.\n\n**B2. `storage.wipe: false` still destroys the disk in AutoYaST.** `:412` emits `<use>all</use>` unconditionally. The AutoYaST guide: `all` \"uses the whole device while calculating the new partitioning\"; `free` means \"only unused space on the device is used, no existing partitions are touched\". `<initialize>false</initialize>` does not undo it. The Agama profile *does* honor `wipe: false` (no `{\"search\":\"*\",\"delete\":true}` entry, `:303-304`), so the two outputs disagree about whether the disk gets erased.\n\n**B3. `installer.on_finish: \"poweroff\"` becomes \"stay\".** `:368` maps only `== \"reboot\"` to `final_reboot`; `poweroff` yields `final_reboot=false`, i.e. the machine idles. AutoYaST's poweroff switch is `<general><mode><halt>`. Two more problems in the same three lines: omitting `on_finish` defaults to `reboot` (`:368`) while `spec/schema.md` §16 says the default is `stay`; and `stay` is refused outright (`:369-370`). Nothing about `on_finish` reaches the Agama profile at all.\n\n**B4. `network.firewall.enabled: false` installs and enables the firewall.** `lis_common.py:930` tests the dict for truthiness, never `enabled`. Verified: `{\"enabled\": false}` → `zypper --non-interactive in firewalld` + `systemctl enable firewalld`. Only a generic tracker line (\"network.firewall.enabled … never reads it\") covers it, which reads as a no-op, not an inversion.\n\n**B5. The duplicate-`/boot` fix is only half applied.** `claimed_mounts` (`:353`, `:445-448`) exists solely in `render_autoyast()`. `agama_drives()` calls `role_mountpoint(part)` at `:322` with no dedup. Verified on `docs/examples/test-raid.lis.json`: `profile.json` contains two partitions with `\"path\": \"/boot\"` (one per disk), which is the original bug, unfixed on the Agama side.\n\n**B6. RAID/LVM member partitions become empty objects in Agama.** Same run: `praid1`/`praid2` (`size: \"rest\"`, no `fs`) emit `{}` — `size_str(\"rest\")` returns `None` (`:41`) so no `size` key, and `fs is None` short-circuits at `:319-321`.\n\n**B7. Two different products from one document.** Agama silently defaults `product.id` to `\"Tumbleweed\"` (`:226`); AutoYaST warns and defaults to `\"Leap\"` (`:484-488`). Same input, two distributions, one warning.\n\n**B8. Subvolume names land under two conventions at once.** `s[\"name\"].lstrip(\"@\") or \"@\"` (`:334`, `:478`) leaves `\"@\"` as `@` but turns `\"@home\"` into `home`, so siblings from one declaration are emitted inconsistently. `lstrip` also strips every leading `@`, not one.\n\n**B9. `system.security.module` installs a package and stops.** `security_packages()` (`lis_common.py:704-720`) adds `apparmor-parser` or `selinux-policy`; nothing enables the LSM (no `security=`/`apparmor=1` kernel parameter, no AutoYaST `<security>` resource, no SELinux relabel). Silent partial honoring of an explicit intent.\n\n**B10. `proxy` and `mirror.url` are post-install only.** `:131-147` writes `/etc/sysconfig/proxy` and runs `zypper ar` inside the chroot. `spec/schema.md` §14 says both apply to the installation *and* the installed system. AutoYaST has a `<proxy>` resource and `<add-on>`; Agama has `product.addons`. The install itself ignores both.\n\n**B11. LVM swap volumes get no mount entry.** `:598` emits `<mount>` only when the volume declares `mountpoint`, while the partition path special-cases `fs == \"swap\"` → `<mount>swap</mount>` at `:452`. Verified in `out/test-lvm-btrfs/autoyast.xml`: the `swap` LV has `<filesystem>swap</filesystem>` and no `<mount>`. I did not verify whether YaST still adds it to fstab, so treat the consequence as unconfirmed.\n\n**B12. Reporting noise that hides real signal.** `packages_of()` and `collect_scripts()` run twice (`:222`/`:358`, `:223`/`:359`) and `refuse()` does not dedupe (`lis_common.py:40-43`) — verified: `software.role: \"desktop:hyprland\"` prints the same refusal twice and reports \"2 core intent item(s)\". Separately, `report()` at `:686` prints the warning count before `check_unread()` runs at `:694`, so the printed count is always wrong (21 vs ~75 on my kitchen-sink document). And `check_section_fields(doc, \"desktop\", {\"bluetooth\",\"printing\"})` at `:673` warns \"desktop.display_manager is not applied by this applier\" when `chroot_intents()` (`lis_common.py:910-916`) does apply it.\n\n**B13. `YAST_KEYMAP` covers 7 layouts** (`:32-33`); anything else passes through raw, so `\"cz\"` is emitted where YaST expects a YaST keymap name. Unverified against YaST's keymap table, but the table's existence implies raw X codes are not accepted.\n\n**B14. `size: \"rest\"` in Agama emits no `size` key** (`:313`). Agama's documented grow-to-fill is a range with `min` and no `max`; omitting `size` hands sizing to the product defaults. Unverified against a running Agama. Note also `size_str`'s docstring claims it returns `'8 GiB'` while it returns `8G`.\n\n## C. UNUSED INSTALLER CAPABILITY — expressible in LIS today\n\n**AutoYaST** (the profile the harness boots). Every one of these has a LIS field that currently only produces a tracker warning or a shell workaround:\n\n| LIS field | AutoYaST element |\n|---|---|\n| `software.services.enable/disable` | `<services-manager>` |\n| `network.interfaces` (addresses/gateway/dns/dhcp) | `<networking><interfaces>`, `<routing>`, `<dns><nameservers>` |\n| `network.hosts` | `<host><hosts>` |\n| `network.firewall` | `<firewall>` resource (currently `firewall-cmd` shell) |\n| `system.time.ntp/servers` | `<ntp-client>` |\n| `system.kdump` | `<kdump>` |\n| `registration.*` | `<suse_register>` (`reg_server`, `email`, `reg_code`) |\n| `proxy` | `<proxy>` (install-time) |\n| `mirror.url` | `<add-on><add_on_products>` (install-time) |\n| `boot.loader` / `os_prober` / `password_hash` / `secure_boot` / `console.serial` | `<bootloader><global>`: `loader_type`, `os_prober`, `password`, `secure_boot`, `serial`, `terminal` — all confirmed present in the guide's GRUB 2 section |\n| `users[].uid` | `<user><uid>` (currently a post-install `usermod`, `lis_common.py:723-744`) |\n| `users[].ssh_authorized_keys` | `<user><authorized_keys>` |\n| `system.extra_locales` | `<language><languages>` (currently warned \"not generated on this distro\") |\n| `system.hwclock` | `<timezone><hwclock>` (currently an `/etc/adjtime` echo) |\n| `partitions[].mount_options` / `label` | `<fstopt>` / `<label>` |\n| `partitions[].existing` (+`format`,`resize`) | `<create>false</create>`, `<partition_nr>`, `<resize>` — currently **refused** at `:306` on Agama's behalf even though AutoYaST can do it |\n| `storage.lvm[].volumes[].subvolumes` | `<subvolumes>` on an LV `<partition>` (dropped in both outputs today) |\n| `scripts.post_storage` / `pre_reboot` | `postpartitioning-scripts` / `post-scripts` |\n| `installer.interactive` / `answers` | `<ask-list>` |\n| `installer.on_finish: poweroff` | `<general><mode><halt>` |\n| `software.exclude` | `<software><remove-packages>` (currently `zypper rm` at post) |\n\n**Agama**: `storage.drives[].partitions[].encryption.luks2` (← `storage.encryption`), `storage.volumeGroups` (← `storage.lvm`), `storage.mdRaids` (← `storage.raid`), `bootloader.timeout` / `bootloader.extraKernelParams` (← `boot.timeout` / `boot.kernel.params`, currently `sed` on `/etc/default/grub`), `product.registrationCode/Email/Url` (← `registration`), `network.connections` (← `network.interfaces`), `files` with `destination/content/permissions/user/group` (← `files[]`, currently base64 echo), `scripts.postPartitioning` (← `scripts.post_storage`), `user.autologin` (← `desktop.autologin`, currently warned \"not applied\").\n\n## D. SPEC GAPS — installer can do it, the schema cannot say it\n\n- **Base product / media identity.** AutoYaST `<software><products>` and Agama `product.id` are effectively mandatory (\"None or wrong base product\" stalls the install, per the comment at `:481-483`), and LIS has no field. The applier is forced into `x-suse.product` — which B1 then refuses. This is the one gap that is actively breaking today.\n- **Encryption tuning**: Agama `luks2` takes `cipher`, `keySize`, `pbkdFunction`, `label`, `tpm`; `storage.encryption[]` carries only `type`/`key`/`unlock`.\n- **fstab reference policy**: AutoYaST `<mountby>` (uuid/label/device/path) — no LIS vocabulary.\n- **Btrfs default subvolume**: AutoYaST `<btrfs_default_subvolume>`. LIS encodes the `@` convention inside subvolume *names*, which is precisely what forces the `lstrip(\"@\")` mangling in B8. A first-class field would remove the guess.\n- **Password aging**: AutoYaST `<user><password_settings>` — `users[].password` has only `hash`/`locked`.\n- **Installer chattiness**: AutoYaST `<report>` (which pop-up classes to suppress) and `<general><self_update>`; `installer.on_error: prompt` is adjacent but not the same knob.\n- **Extra repositories / add-on products**: `docs/coverage-matrix.md:53` already parks these in `x-*`, but `mirror.url` half-covers the case and lands post-install (B10).\n- **Enterprise device activation**: Agama `iscsi` / `dasd`, AutoYaST `<samba-client>` / `<nfs-client>`. `docs/coverage-matrix.md:68` already flags iSCSI/DASD as a planned `target` extension.\n- **Agama `security.sslCertificates`** — no LIS field.\n\n## Reproduction\n\nGenerated fixtures are at `/tmp/nix-shell.ouGjCL/claude-1000/-home-bresilla-lis/67872401-f199-4cc3-ab7b-7d06474ee4e8/scratchpad/` (`t1.lis.json` covers A2/A3/A4/A5/B3/B4; `t2.lis.json` covers B1/B2; `out-test-encrypted/` covers A1; `out-test-raid/` covers B5/B6).\n\nSources: [AutoYaST guide](https://doc.opensuse.org/projects/autoyast/), [Agama storage reference](https://agama-project.github.io/docs/user/reference/profile/storage), [SLES 16 Agama automated installation](https://documentation.suse.com/sles/16.0/html/SLES-x86-64-agama-automated-installation/index.html)",
      "All archinstall citations are relative to `/tmp/archx/full/usr/lib/python3.14/site-packages/archinstall/` (4.4 from the ISO). Applier citations are `/home/bresilla/lis/tools/appliers/lis2archinstall.py` unless noted.\n\n---\n\n# A. SILENT DROPS\n\n### A1. `storage.raid` + `storage.encryption` ⇒ the disk is installed unencrypted, no refusal\n`translate()` branches at :570 — if `storage.raid` is present it takes `raid_disk_config(doc)` and **never enters the `elif dc := disk_config(doc)` branch (:580–636) that is the only place `disk_encryption` is ever attached**. `raid_disk_config()` (:242–409) has no encryption code at all, and the key-material check at :125–134 lives inside `disk_config()`, so it never runs either.\n\nVerified: I took `docs/examples/test-raid.lis.json`, added `storage.encryption: [{id: cryptmd, over: md0, key:{passphrase:true}, unlock:[passphrase]}]` and pointed `lvm[].devices` at the container. Result: **0 refusals**, `disk_encryption` absent from `user_configuration.json`, `encryption_password` absent from creds, and the sole warning was `storage.encryption[].type is declared but this applier never reads it`. `check_raid_consumers()` passes because `encryption[].over` names the array. `backing()` (:94–110) even resolves the container down to the array so the VG lands on the *bare* array — the container silently evaporates. This is the highest-risk item in the file: the operator asked for full-disk encryption and gets a plaintext disk with a cosmetic warning.\n\n### A2. `software.role: \"desktop:gnome\"` installs no desktop at all\n`ROLE_MAP` (:59) maps it to `\"Gnome\"`, emitted as `profile.details: [\"Gnome\"]` (:659). archinstall's GNOME profile is named `'GNOME'` (`default_profiles/desktops/gnome.py:9`) and `get_profile_by_name` is an exact string compare (`lib/profile/profiles_handler.py:136`). A miss is appended to `invalid_sub_profiles` and reported with `info()` only (`profiles_handler.py:96,99`) — the install continues with `DesktopProfile.current_selection == []`, which installs `nano vim openssh htop wget smartmontools xdg-utils` and nothing else (`default_profiles/desktop.py:24–34,101–116`): no `gnome`, no Xorg/Wayland session, no default greeter.\n\n`\"Kde\"` survives only because archinstall has an explicit back-compat rewrite to `'KDE Plasma'` (`profiles_handler.py:88–91`). `Hyprland`, `Sway`, `Xfce4`, `Cinnamon`, `Mate`, `Budgie` match exactly. So `desktop:gnome` — a role in the spec's own registry (`spec/schema.md:416–418`), and the role used by `docs/examples/desktop-encrypted.lis.yaml` — is the single broken one, and no e2e recipe exercises it.\n\n### A3. `drivers.gpu` — one value silently null, two values abort archinstall\n`gfx_driver()` (:1014–1017) keys on `\"amd\"`, but the schema enum value is **`amdgpu`** (`spec/schema.json`). Verified: `drivers.gpu: amdgpu` → `\"gfx_driver\": null`. `drivers.gpu` *is* read (by `driver_packages`), so `check_unread` stays quiet — a silent drop the tracker cannot see.\n\nThe other two are worse than silent:\n- `\"nvidia\"` → `\"Nvidia (proprietary)\"`, which `ProfileConfiguration.parse_arg` raises on by name (`lib/models/profile.py:60–64`), and `ArchConfig.from_config` turns that into `warn(); sys.exit(1)` (`lib/args.py`, `ArchConfigHandler.__init__`).\n- `\"nvidia-open\"` → `\"Nvidia (open kernel module)\"`, which is not a `GfxDriver` member — the real value is `'Nvidia (open kernel module for newer GPUs, Turing+)'` (`lib/hardware.py:60`) — so `GfxDriver(...)` raises the same way.\n\n### A4. `desktop.display_manager` values `lightdm` / `greetd` / `none` abort archinstall\n:657 passes the LIS string straight through as `greeter`. `GreeterType` (`default_profiles/profile.py:32–40`) has `'lightdm-gtk-greeter'`, `'lightdm-slick-greeter'`, `'sddm'`, `'gdm'`, `'ly'`, `'cosmic-greeter'`, `'plasma-login-manager'`, `'dms-greeter'`. `gdm`/`sddm`/`ly` work; `lightdm`, `greetd` and `none` are not members and make `GreeterType(greeter)` raise → archinstall exits 1 before touching anything. Only reachable with a `desktop:*` role.\n\n### A5. `unlock: [tpm2]` / `[fido2]` enroll a key slot the initramfs cannot use\n`enrollment_commands()` (`lis_common.py:790–828`) runs `systemd-cryptenroll --tpm2-device=auto` as a custom command and the applier warns \"must be enrolled after installation\" (:130–133). But archinstall only inserts the **`encrypt`** mkinitcpio hook unless `disk_encryption.hsm_device` is set, in which case it installs `libfido2` and uses **`sd-encrypt`** (`lib/installer.py:890–899`). The applier never emits `hsm_device` (`lib/models/device.py:1479`). Net effect: the LUKS header gets a TPM2/FIDO2 slot, the boot path still only knows how to prompt for a passphrase. The warning says the opposite of what happens.\n\n### A6. `software.flatpak[]` and `apps[].flatpak` — nothing is ever installed\n:531–533 adds the `flatpak` *package* when `apps[].flatpak` is truthy and then discards the app ID; `software.flatpak[]` is never read at all (warned by `check_unread`). Verified: `apps: [{name: vscode, package: code, flatpak: com.visualstudio.code}]` → `packages` gains `code` and `flatpak`, `custom_commands` contains no `flatpak install`. Peer appliers prove this is honorable — `lis2kickstart.py:473–474` and `lis2debian.py:805–814` emit `flatpak install -y --noninteractive flathub <app>`. Because `apps[].flatpak` is *read*, the tracker never flags it.\n\n### A7. `system.security.module: apparmor` installs a package that never activates\n`security_packages()` (`lis_common.py:704–720`) adds the `apparmor` package and stops. AppArmor on Arch needs `lsm=...,apparmor` on the kernel command line and `apparmor.service` enabled; neither is emitted, and the applier does own the kernel command line (:763–778) and `services`. No warning.\n\n### A8. `storage.snapshots.boot_menu: true` installs grub-btrfs but never enables it\n`chroot_intents()` (`lis_common.py:956–961`) emits `pacman -S grub-btrfs`. archinstall's own path (`lib/installer.py:1024–1029`) also straps `inotify-tools`, runs `_configure_grub_btrfsd()` and enables `grub-btrfsd.service`. Without the daemon the GRUB submenu is never regenerated, so no snapshot entries appear. Same helper's snapper setup (`snapper -c root create-config /`) omits the `home` config and both `snapper-timeline.timer` / `snapper-cleanup.timer` that archinstall enables (`installer.py:999–1015`).\n\n### A9. `software.services.disable[]` and `network.ssh.*` reach nothing\nBoth are `check_unread`-warned, never emitted. archinstall has `Installer.disable_service` (`lib/installer.py:735`) but no config key for it — however the applier's `custom_commands` channel makes `systemctl disable X` trivial, and it already emits dozens of such commands. `network.ssh.{enabled,password_auth,permit_root}` is handled by `lis2kickstart.py:328` and `lis2nixos.py:795`; here it is dropped whole. `network.hosts[]` likewise (handled at `lis2nixos.py:783`).\n\n### A10. `fs: \"zfs\"` and a plain partition/volume with no `fs` produce a profile archinstall rejects\n`FS_MAP` (:55) has no `zfs`, and `FS_MAP.get(fs, fs)` passes it through. Verified: `\"fs_type\": \"zfs\"` is emitted with **no refusal**. `FilesystemType` (`lib/models/device.py:818–833`) has no zfs member, so `FilesystemType('zfs')` raises during `parse_arg`. Same class of problem at :470: an LVM volume with `fs` absent or `\"none\"` emits `\"fs_type\": null`, and `LvmVolume.parse_arg` does an unguarded `FilesystemType(arg['fs_type'])` (`device.py:1207–1215`).\n\n---\n\n# B. DRIFT\n\n### B1. `boot.uki: true` is emitted as `\"uki\": false`\n:559 hardcodes `\"uki\": False`. `BootloaderConfiguration.uki` is a real, fully wired field (`lib/models/bootloader.py:94`; drives `run_mkinitcpio` and `_config_uki` in `scripts/guided.py:75` / `lib/installer.py:1783`). `check_boot_extras` does warn, but the profile actively asserts the opposite of the declared value rather than omitting it. Verified.\n\n### B2. zram is switched on for documents that never mention swap\n:564: `\"swap\": bool((storage.get(\"swap\", {}) or {}).get(\"zram\", True))`. With no `storage.swap` key at all this is `True` — verified. archinstall's own default is `swap: ZramConfiguration | None = None` (`lib/args.py:167`) and it only calls `setup_swap()` when the key is present (`scripts/guided.py:114`), so the applier is inventing zram-generator + `systemd-zram-setup@zram0.service` that the document did not ask for. Separately, `storage.swap.zram` is an *object* in the schema, so `bool({...})` also means `zram: {size: ...}` is indistinguishable from `zram: true` — and `zram.size` is dropped (warned; archinstall's `ZramConfiguration` has no size field either, so that half is a genuine installer gap).\n\n### B3. `network.manager: systemd-networkd` becomes \"copy the ISO's network config\"\n:643–645 emits `{\"type\": \"iso\"}`, which runs `copy_iso_network_config()` (`lib/installer.py:781`) — it copies whatever the live ISO happened to have. archinstall's actual systemd-networkd path is `NicType.MANUAL`, which writes per-interface `.network` units and enables `systemd-networkd` + `systemd-resolved` (`lib/network/network_handler.py:44–49`). The warning says \"archinstall keeps the ISO network config\" but does not say the declared interfaces are gone.\n\n### B4. `role: esp` with no explicit mountpoint gets `/boot/efi` from the role table\n:202 `role_mountpoint(part) or \"/boot\"` — the `or \"/boot\"` is dead, because `ROLE_MOUNT[\"esp\"]` is `/boot/efi` (`lis_common.py:54`). With a single ESP and no separate boot partition, `get_boot_partition()` and `get_efi_partition()` both return it (`lib/models/device.py:1416–1424`, both are flag-based), so `boot_partition == efi_partition` and `_add_systemd_bootloader` skips `--esp-path/--boot-path` (`lib/installer.py:1264–1266`) while the ESP is mounted at `/boot/efi`. I could not run archinstall here to confirm the outcome, so I am flagging this as a role-derived value the document did not ask for, not as a proven failure.\n\n### B5. `desktop.audio: \"none\"` is refused although archinstall supports it\n:666–670 refuses anything outside pipewire/pulseaudio/auto. `Audio.NO_AUDIO = 'No audio server'` (`lib/models/application.py:24`) and `ApplicationHandler` explicitly skips installing an audio server for it (`lib/applications/application_handler.py`). Also `auto` → `pipewire` is an invented choice, silently.\n\n---\n\n# C. UNUSED INSTALLER CAPABILITY (expressible in LIS today)\n\nNothing in this list is emitted — confirmed by `grep -n \"app_config\\|mirror_config\\|auth_config\\|pacman_config\\|btrfs_options\\|hsm_device\\|console_font\\|nics\\|custom_servers\" lis2archinstall.py` → no hits.\n\n| LIS field | archinstall key it maps to | Applier today |\n|---|---|---|\n| `mirror.url` | `mirror_config.custom_servers: [{url}]` → prepends `Server = <url>` to the mirrorlist on live **and** target (`lib/installer.py:601–607`, `lib/models/mirrors.py:243,251`) | **refuses the whole `mirror` section** (:672–674). This applier is also the only one that does not import `check_mirror` |\n| `mirror.country` | `mirror_config.mirror_regions: {\"<country>\": [...]}` (`mirrors.py:296–301`) — note `regions_config` looks the name up in the fetched status list, so an unknown country KeyErrors | same blanket refusal |\n| `network.interfaces[]` (`match.name`, `dhcp4`, `addresses`, `gateway`, `dns`) | `network_config: {\"type\": \"manual\", \"nics\": [{iface, ip, dhcp, gateway, dns}]}` (`lib/models/network.py:38–95`; `Nic.as_systemd_config` writes the `.network` unit) | never read; `check_unread` warning only |\n| `network.manager: iwd` | `NicType.IWD` (`network.py:14`), which configures `/etc/iwd/main.conf` + a wired `.network` and enables iwd/networkd/resolved (`network_handler.py:36–43`) | refused at :647 as \"not selectable in archinstall\" |\n| `desktop.bluetooth: true` | `app_config.bluetooth_config: {enabled: true}` → `bluez`, `bluez-utils`, `bluetooth.service` (`lib/applications/bluetooth.py`) | `check_section_fields` warning only |\n| `desktop.printing: true` | `app_config.print_service_config: {enabled: true}` → `cups`, `system-config-printer`, `cups-pk-helper`, `cups.service` (`lib/applications/print_service.py`) | warning only |\n| `network.firewall.enabled` | `app_config.firewall_config: {firewall: \"ufw\"\\|\"firewalld\"}` — installs, enables, **and flips `ENABLED=no`→`yes` in `/etc/ufw/ufw.conf`** (`lib/applications/firewall.py:36–52`) | hand-rolled `pacman -S ufw; systemctl enable ufw` custom commands (`lis_common.py:930–942`) that never set `ENABLED=yes`, so ufw is enabled-but-inactive |\n| `storage.snapshots.tool: \"timeshift\"` | `disk_config.btrfs_options.snapshot_config.type = \"Timeshift\"` → straps `cronie` + `timeshift` (`lib/models/device.py:1357–1372`, `lib/installer.py:1017–1022`) | `check_snapshots(doc, tools={\"snapper\"})` **refuses** it (:1070) |\n| `storage.snapshots.enabled` (snapper) | same `btrfs_options`, gets root+home configs, both timers, and full grub-btrfsd wiring for free | shell reimplementation, see A8. Caveat: `btrfs_options` is only parsed when `has_default_btrfs_vols()` is true, i.e. a **partition** (not an LVM volume) carries a subvol named `@` at `/` (`device.py:232–246`) |\n| `storage.encryption[].unlock: [fido2]` + `keys[].type: fido2` | `disk_config.disk_encryption.hsm_device` (`device.py:1479`, `Fido2Device` at :1575) — the only thing that switches mkinitcpio to `sd-encrypt` | see A5 |\n| `system.keymap.font` | `locale_config.console_font` → `/etc/vconsole.conf` `FONT=`, auto-straps `terminus-font` for `ter-*` (`lib/models/locale.py:18`, `lib/installer.py:2031–2050`) | `check_keymap` warning only |\n| `software.role: \"server:<x>\"` | archinstall ships server sub-profiles: cockpit, docker, httpd, lighttpd, mariadb, nginx, postgresql, sshd, tomcat (`default_profiles/servers/`) — `{\"main\":\"Server\",\"details\":[\"Docker\"]}` | :662 emits `details: []` for `server`, refuses anything else |\n| `software.role: desktop:*` beyond the 8 mapped | Awesome, Bspwm, Cosmic, Deepin, Enlightenment, i3, Labwc, Lxqt, niri, Qtile, River, Xmonad (`default_profiles/desktops/`) | refused at :663–664 |\n\nReachable through `custom_commands` (archinstall runs each as a chroot script *after* `add_bootloader`, `scripts/guided.py:181–182`), which the applier already uses for `raid_commands`, GRUB timeout and kernel params — so the channel is proven, only the content is missing: `boot.os_prober` (`GRUB_DISABLE_OS_PROBER`), `boot.password_hash`, `boot.console.serial` (`GRUB_TERMINAL`/`GRUB_SERIAL_COMMAND`), `boot.kernel.modules` / `boot.kernel.blacklist` (`/etc/modules-load.d`, `/etc/modprobe.d`), `boot.initramfs.include_modules` (sed `MODULES=` + `mkinitcpio -P` — exactly the shape of `raid_commands()` at :412–432), `system.time.servers`/`provider`, `system.domain`, `desktop.autologin`. All currently `check_unread`/`check_boot_extras` warnings.\n\n---\n\n# D. SPEC GAPS (archinstall can, the schema cannot say)\n\n- `bootloader_config.plymouth` — 10 themes (`lib/models/bootloader.py:63–87`), wired through `add_bootloader` → `_install_plymouth` (`installer.py:1761`). No LIS field.\n- `auth_config.u2f_config: {u2f_login_method: passwordless|second_factor, passwordless_sudo: bool}` (`lib/models/authentication.py:22–57`). LIS `keys[].purpose` has `user_pam_auth` but no way to bind it to a login method.\n- `app_config.fonts_config` (noto / noto-cjk / noto-emoji / ttf-liberation / ttf-dejavu) and `app_config.power_management_config` (power-profiles-daemon / tuned) — `lib/models/application.py:44–60,10–12`. No LIS vocabulary.\n- `mirror_config.optional_repositories` (`multilib`, `*-testing`) and `mirror_config.custom_repositories` (name/url/SigLevel) — `lib/models/packages.py:11–18`, `mirrors.py:169–200`. `spec/schema.md:434–435` explicitly pushes repositories to `x-*`, and the applier's `x-arch` handling covers `packages` only (:787–789).\n- `pacman_config.{parallel_downloads,color}` (`lib/models/pacman.py:14–16`).\n- `disk_encryption.iter_time` (`device.py:1480`), LUKS PBKDF cost.\n- `swap.algorithm` (zstd/lzo-rle/lzo/lz4/lz4hc, `application.py:70–75`) — LIS has `storage.swap.zram.size`, which archinstall does *not* have, and lacks the algorithm it does have.\n- `storage.partitions[].label` — genuinely absent from `_PartitionModificationSerialization` (`device.py:865–878`); the existing warning is correct.\n- `subvolumes[].mount_options` — `SubvolumeModification` is `{name, mountpoint}` only (`device.py:658–661`); correct warning.\n- `storage.swap.file` — `Installer.add_swapfile()` exists (`installer.py:529`) but `scripts/guided.py` never calls it and no config key reaches it; only `custom_commands` could.\n- `Bootloader.Refind` and `NicType.NM_IWD` have no LIS spelling (`boot.loader` and `network.manager` enums are closed).\n\n---\n\n### One structural note\n`check_unhandled(doc, ALL_SECTIONS)` (:1037) passes *every* intent section as \"handled\", so the lis_common safety net that is supposed to make a new spec section loud is a no-op here. That is a project-wide pattern (all seven appliers do it), not archinstall-specific, but it is why A1/A2/A3/A6 pass with zero refusals.",
      "Verified against disko `de57087` (`/nix/store/z2bsz1xw44k3gbkb4ks4ds8p0gyspvwb-source`) and nixpkgs 25.05 (`/nix/store/3p306srz83h9z9v0ma9xcxb8y8cdxkxj-source`) — the release `lis2nixos` pins in `system.stateVersion`. All applier paths are `/home/bresilla/lis/tools/appliers/lis2nixos.py` unless noted.\n\n---\n\n# A. SILENT DROPS — declared, neither emitted nor refused\n\n**A1. `software.exclude` — dropped by a comment that names a function this applier never calls.**\n`:916-917` reads it and does `pass   # honored by chroot_intents()`. `lis2nixos.py` never imports or calls `chroot_intents` (grep: the string appears only in those two comments; the import block `:23-26` does not list it). Because `.get()` marks the path read, the tracker stays silent too — confirmed: my kitchen-sink run produced **no** message of any kind about `software.exclude`. NixOS can express most of it (`environment.defaultPackages`, nixpkgs `config/system-path.nix:83`).\n\n**A2. `users[].dotfiles` — same dead comment.** `:875-876` `pass   # honored by chroot_intents()`. Here the tracker does catch the leaves (`users[].dotfiles.repo` / `.method` warned), but nothing refuses, and the applier already owns an activation-script + firstboot mechanism (`render_script_hooks`, `:621-688`) that could clone the repo.\n\n**A3. `storage.raid[].spares` becomes an active array member.** `Topology.__init__` `:113-115` puts `devices + spares` into the same `consumer` map, so a spare partition gets `content = { type = \"mdraid\"; }`. `render_mdadm` `:219-221` emits only a Nix **comment**: `# 1 spare(s) declared: esp2 — disko marks them by device order`. That claim is false: disko's mdadm creator (`lib/types/mdadm.nix:61-76`) runs `mdadm --create … --raid-devices=\"$(wc -l …)\"` over every member device — there is no spare option in `mdadm.nix` at all. A `level: 1, devices: [a,b], spares: [c]` document silently becomes a 3-way mirror. No `warn()`, no `refuse()`.\n\n**A4. `role: root` / `role: boot` without an explicit `fs` produce an unformatted, unmounted partition.** `emit_layer` `:199-200` defaults fs only for `role: swap`; `mount_table.add` `:519` uses `{\"esp\":\"vfat\",\"swap\":\"swap\",\"root\":\"btrfs\"}` and no `boot` entry, and only defaults a *mountpoint* for `esp` (`:556`). `lis_common.ROLE_FS`/`ROLE_MOUNT` (`lis_common.py:53-54`) say boot→ext4→`/boot`, root→btrfs→`/`; this applier ignores both tables. Reproduced: a document with `{role: boot, size: 1GiB}` and `{role: root, size: rest}` generated `\"boot1\" = { size = \"1G\"; };` and `\"root3\" = { size = \"100%\"; };` with **no `content`**, a `hardware.nix` with **no `fileSystems.\"/\"`**, and `0 warning(s), 0 refusal(s)`. The result is an unbootable machine from a document the applier claims to have honored. (Spec §20.5 \"exactly one mountpoint `/` resolves\" is never checked here either.)\n\n**A5. `mount_options` on a `role: esp` partition are discarded.** `render_disko` `:335-343` short-circuits the whole topology for ESPs and hardcodes `mountOptions = [ \"umask=0077\" ]`, ignoring `part[\"mount_options\"]` and `part[\"fs\"]`. `mount_table` (`:526`) uses the declared options. Reproduced: declared `[\"fmask=0022\",\"dmask=0022\"]` → `disko.nix` says `umask=0077`, `hardware.nix` says `fmask/dmask`. Zero warnings.\n\n**A6. Root account loses `groups` and `admin`.** `:842` guards `extraGroups` with `if user[\"name\"] != \"root\"`. Reproduced: `{name: root, admin: true, groups: [\"docker\"]}` produced `users.users.root = { hashedPassword = \"!\"; shell = pkgs.zsh; }` — no `extraGroups`, no warning. `admin` is read at `:840` and thrown away for root.\n\n**A7. `storage.swap.file.size` is rounded to a hardcoded 4 GiB unless it ends in `GiB`.** `:989-992`: `gib = int(size[:-3]) if size.endswith(\"GiB\") else 4`. A schema-legal `\"512MiB\"` or `\"1TiB\"` (`$defs/size` allows all three units) silently becomes `size = 4096`. No warning.\n\n**A8. `storage.swap.zram.size` dropped.** `:987-988` emits only `zramSwap.enable = true`. `zramSwap.memoryPercent` exists (nixpkgs `nixos/modules/config/zram.nix:50`) and `\"50%\"` maps to it exactly. Tracker-warned only.\n\n**A9. `proxy.https` dropped.** `:824-825` emits `networking.proxy.default = <proxy.http>`, which nixpkgs then propagates to `httpsProxy` (`nixos/modules/config/networking.nix:90,98-100`). A document whose https proxy differs from its http proxy gets the http one, silently. `networking.proxy.httpsProxy` is right there.\n\n**A10. `users[].scripts.post` never read.** `render_script_hooks` `:634-647` collects only `post_install` and `firstboot` per user. Tracker-warned; no refusal.\n\n**A11. `system.telemetry: \"off\"` never read.** Nothing consults it. NixOS ships no telemetry so the intent is vacuously satisfied, but §2.3 wants that said, not assumed.\n\n---\n\n# B. DRIFT — emitted config differs from what was declared\n\n**B1. Partition names diverge between `disko.nix` and `hardware.nix` whenever an `existing` adoption is present.** `render_disko` `:326-331` `continue`s past `existing` partitions *before* `index += 1`; `mount_table` `:542-545` and `luks_initrd_devices` `:455-459` increment unconditionally. Reproduced with `[esp(existing), boot, swap, root]`: disko created `boot1 / swap2 / root3`, while `hardware.nix` emitted `swapDevices = [{ device = \"/dev/disk/by-partlabel/disk-main-swap3\"; }]` — a device that will never exist. Worse, the adopted ESP that disko *skipped* still got `fileSystems.\"/boot\" = \"/dev/disk/by-partlabel/disk-main-esp1\"`. The whole run: `1 warning(s), 0 refusal(s)`. `existing` should be refused, not warned-and-skipped.\n\n**B2. Partition labels longer than 36 characters: the applier guesses wrong.** disko's `gpt.nix:139-155` uses `disk-<disk>-<part>` **only if ≤ 36 chars**, otherwise `substring 0 36 (hashString \"sha256\" label)`. `mount_table` `:547` and `luks_initrd_devices` `:459` hardcode the unhashed form with no length check. Reproduced with disk id `primary-nvme-system` and partition id `root-filesystem-vol` (both valid `$defs/handle`): disko will label it `0952f545dfa3a7ae829ed0c94f2a540262f7`, `hardware.nix` points at `/dev/disk/by-partlabel/disk-primary-nvme-system-root-filesystem-vol`. Unbootable; `0 warning(s), 0 refusal(s)`.\n\n**B3. A `role: esp` partition consumed by RAID/LVM/LUKS is formatted vfat and mounted `/boot` anyway.** The `role == \"esp\"` branch (`:335-343`) never calls `topology.emit_content`. Reproduced: two ESPs, the second listed in `raid[].spares` → **both** disko partitions carry `mountpoint = \"/boot\"`. `mount_table` correctly skips the consumed one, so `hardware.nix` mounts only one. This is the same class as the `role_mountpoint()` bug named in the brief, on the disko side.\n\n**B4. `boot.kernel.variant: \"realtime\"` emits an attribute that does not exist.** `:802-804` maps realtime→`linuxPackages_rt`, emitted as `boot.kernelPackages = pkgs.linuxPackages_rt;`. In nixpkgs 25.05 the attribute is `linuxPackages-rt` (`pkgs/top-level/all-packages.nix:11270`); `linuxPackages_rt` does not exist (verified by `nix-instantiate --eval`: attribute missing; `pkgs/top-level/aliases.nix` only has the versioned `linuxPackages_rt_5_10` etc.). Eval error at `nixos-install` time — i.e. **after** `--apply` has already run `disko --mode disko` and destroyed the disks (`:1053-1066`).\n\n**B5. Any `${` in a document string produces broken Nix.** `nix_str` `:29-30` escapes `\\` and `\"` but not `${`, which opens antiquotation inside a Nix `\"…\"` string. Reproduced: `files[{path:\"/etc/profile.d/x.sh\", content:\"echo ${HOME}\\n\"}]` → `environment.etc.\"profile.d/x.sh\".text = \"echo ${HOME}\"` → `error: undefined variable 'HOME'`. `nix_str` is also used for `comment`, `mount_options`, `ssh_authorized_keys`, `label`, `hostname`. (`nix_script` `:589-591` does escape correctly — so hook bodies are safe, file contents are not.)\n\n**B6. Package names are interpolated unquoted.** `:913-915` emits `environment.systemPackages = with pkgs; [ {' '.join(pkgs_list)} ];`. Reproduced: `software.packages: [\"7zip\",\"gtk+\",\"python3-pip\"]` → `nix-instantiate --parse` fails with `syntax error, unexpected '+'`. Same destroy-then-fail ordering as B4.\n\n**B7. `size: \"50%\"` on a partition is invalid disko.** `disko_size` `:45-46` passes percentages through. disko's `gpt.nix:158-160` types `size` as `either (enum [\"100%\"]) (strMatching \"[0-9]+[KMGTP]?\")` — verified: `t.check \"50%\" == false`. LIS `$defs/sizeOrRest` explicitly permits `[0-9]{1,3}%`. Spec-legal input, un-evaluatable output, no warning.\n\n**B8. `size: \"50%\"` on an LVM volume changes meaning.** `lvm_vg.nix:130-135` rewrites any `%` suffix to `%FREE`, so `lvcreate -l 50%FREE` is 50 % of *remaining free space*, not 50 % of the VG. Two volumes at `\"50%\"` yield 50 % and 25 %.\n\n**B9. LUKS keyfile paths are not resolved, and disagree within one document.** `:154-155` emits `settings.keyFile = <raw keyfile>` and `:419` emits `boot.initrd.luks.devices.<n>.keyFile = <raw keyfile>`, while `lis_common.luks_key_path` (`lis_common.py:781-783`) resolves a relative keyfile to `/run/lis/seed/…`. Reproduced with `key: {keyfile: \"keys/luks-root.key\"}`: disko gets `settings.keyFile = \"keys/luks-root.key\"`, initrd gets the same relative path, and the firstboot `systemd-cryptenroll` line reads `/run/lis/seed/keys/luks-root.key`. disko's own `keyFile` option is typed `absolute-pathname` (`luks.nix:60-64`); `settings` is `attrsOf anything` so the relative path type-checks and then fails at `cryptsetup` time.\n\n**B10. `password.locked: true` discards the hash.** `:848-851` tests `locked` before `hash` and writes `hashedPassword = \"!\"`. `lis_common.password_field` (`:546-558`) returns `!<hash>` — lock now, unlock later. This applier loses the hash permanently.\n\n**B11. Serial getty is invented from kernel params and hardwired to `ttyS0`.** `:731-733`: `if any(p.startswith(\"console=ttyS\") …)` → `systemd.services.\"serial-getty@ttyS0\".enable = true`. `console=ttyS1,115200` gets a getty on the wrong port. Meanwhile the field that actually declares this, `boot.console.serial`, is never read (tracker-warned).\n\n**B12. `desktop.display_manager` is warned as \"not applied\" while a role-derived DM is emitted anyway.** `:888-890` hardcodes gdm for `desktop:gnome`, `:892` sddm for KDE. `display_manager: \"none\"` or `\"sddm\"` beside `role: desktop:gnome` still yields gdm. All five DMs exist in nixpkgs (`services/x11/display-managers/{gdm,lightdm}.nix`, `services/display-managers/{sddm,greetd,ly}.nix`).\n\n**B13. `boot.kernelParams` is emitted twice** — `hardware.nix:395` and `configuration.nix:730`. `listOf str` merges by concatenation, so params are duplicated rather than clobbered. Cosmetic but it is not what the document says.\n\n**B14. Activation hooks re-run on every `nixos-rebuild`.** `:665-673` puts `post_storage/post_install/post/pre_reboot/on_success` into `system.activationScripts.lis-hooks`. Spec §13 defines these as one-shot install-time hooks; here they run on every switch. The docstring acknowledges the mechanism but not the semantic change, and there is no `warn()`.\n\n**B15. Invented ZFS defaults.** `render_zpools` `:242-249` hardcodes pool `rpool`, `compression = \"zstd\"`, `\"com.sun:auto-snapshot\" = \"false\"`, `options.ashift = \"12\"`. Only the `mode = \"mirror\"` guess is warned (`:240-241`).\n\n**B16. Three `check_boot_extras` warnings are false.** `:1019` passes `used = {\"kernel\",\"loader\",\"params\",\"timeout\",\"variant\"}`, so `boot.kernel.modules` and `boot.kernel.blacklist` are reported \"not applied\" even though `render_hardware` `:391-393` emits `boot.kernelModules` and `boot.blacklistedKernelModules`; likewise `boot.initramfs` is reported unapplied although `include_modules` is honored at `:382-384`. Warnings that cry wolf erode the one signal an operator has.\n\n---\n\n# C. UNUSED INSTALLER CAPABILITY — expressible in LIS today\n\n| LIS field | Current | NixOS/disko option that exists |\n|---|---|---|\n| `network.wifi[]` | **refused** `:782` | `networking.wireless.networks.<ssid>.pskRaw` + `.hidden` — `services/networking/wpa_supplicant.nix:281,304,92`. A 1:1 match for `psk_hash`/`hidden`. |\n| `network.interfaces[]` | **refused** `:779` | `networking.interfaces.<n>.ipv4.addresses` / `networking.defaultGateway` / `networking.nameservers`; and `systemd.network.networks.<n>.matchConfig.{Name,MACAddress}` handles `match.name: \"en*\"` globs and `match.mac` exactly. |\n| `boot.password_hash` | warned-dropped | `boot.loader.grub.users.<n>.hashedPassword` — `system/boot/loader/grub/grub.nix:211,241`. |\n| `boot.uki` | warned-dropped | `boot.uki.enable` — `system/boot/uki.nix`. |\n| `boot.kernel.variant: \"zen\"` | **refused** `:802-804` | `pkgs.linuxPackages_zen` — `all-packages.nix:11298` (verified to evaluate). |\n| `desktop.display_manager` | warned-dropped | gdm/lightdm/sddm/greetd/ly modules all present (see B12). |\n| `software.services.disable[]` | **refused** `:927-928` | `systemd.services.<n>.enable = false` works for any unit name. |\n| `software.exclude[]` | silently dropped | `environment.defaultPackages` — `config/system-path.nix:83`. |\n| `storage.swap.zram.size` | dropped | `zramSwap.memoryPercent` — `config/zram.nix:50`. |\n| `proxy.https` | dropped | `networking.proxy.httpsProxy` — `config/networking.nix:98`. |\n| `partitions[].label`, `lvm[].volumes[].label` | dropped | disko `filesystem.extraArgs` (`filesystem.nix:22-26`, `mkfs … ${extraArgs}`) for `-L`, and GPT `label` (`gpt.nix:139`). |\n| `system.kdump` | **refused** `:993-994` | `boot.crashDump.enable` — `nixos/modules/misc/crashdump.nix`. (kexec crash dump, not distro kdump tooling — arguably a partial answer, but it is not \"no NixOS default mapping\".) |\n| `software.flatpak[]` | enabled + warned as runtime-only | The applier already emits a `lis-firstboot` oneshot (`:678-687`); `flatpak install -y <id>` in it would honor the intent. |\n| `installer.on_finish: reboot` | warned-dropped | Trivially honorable on the `--apply` path (`:1044-1073`), which already shells out. |\n| `files[]` outside `/etc` | **refused** `:974-975` | `system.activationScripts` / `systemd.tmpfiles.rules` — and the applier already writes an activation script. |\n| `keys[].match`, `keys[].purpose: user_ssh_key/user_pam_auth` | warned wholesale `:980-983` | `enrollment_commands` uses `--fido2-device=auto`, ignoring `match.serial`; `security.pam.u2f` exists for `user_pam_auth`. |\n| `software.apps[].preference` | dropped | This is the §2.3 substitution-exemption marker; ignoring it means the applier never uses its one licence to substitute, and never reports one (§19 `substitutions[]` is never produced). |\n\nCorrectly refused, verified as genuine absences: `software.snap` (no snapd module anywhere in nixpkgs 25.05), `system.security.module: selinux` (nothing under `nixos/modules/security/`), `registration` (no subscription service), `system.init` (systemd-only), `boot.initramfs.generator` (no dracut/mkinitcpio/booster module). `boot.secure_boot` is likewise absent from nixpkgs (lanzaboote is out-of-tree) — but it is only *warned*, not refused, so `secure_boot: true` silently yields a non-Secure-Boot system.\n\n---\n\n# D. SPEC GAPS — installer can do it, the schema cannot say it\n\n- **disko `swap` tuning**: `priority`, `discardPolicy`, `randomEncryption`, `resumeDevice` (`lib/types/swap.nix:22,51,60,65`). LIS `storage.swap` has only `zram.size` and `file.{path,size}`.\n- **ZFS pool topology**: `zpool.mode` plus `vdev`/`spare`/`log`/`cache`/`special`/`dedup` groups (`zpool.nix:33,85-206`), `rootFsOptions`, `options.ashift`. Schema §6.1 admits this: \"`fs: zfs` is single-filesystem use only in v0.1\". This is why `render_zpools` has to invent `rpool` and guess `mirror`.\n- **MBR / msdos partition table** (`lib/types/table.nix`). `target.firmware: bios` currently forces GPT + a synthetic `EF02` partition (`:288-295`); there is no way to ask for a real msdos table.\n- **GPT partition detail**: `uuid`, `priority`, explicit `start`/`end`, `attributes`, `hybrid` MBR (`gpt.nix:126,103,185,60,206`).\n- **mdadm `metadata` version** (`mdadm.nix:26`); and, conversely, **RAID hot spares** which LIS *does* declare but disko cannot express — this is the pairing behind A3, and the honest resolution is a refusal.\n- **LVM `lvm_type`** — thin pools, `raid1`/`raid5` logical volumes, `extraArgs` (`lvm_vg.nix:57,74`).\n- **Filesystem mkfs tuning** generally (`filesystem.extraArgs`): inode size, block size, `-m` reserve.\n- **Disk image build**: `disk.imageName` / `imageSize` (`disk.nix:30,38`) — building an installable image rather than partitioning a live machine has no LIS vocabulary at all.\n- **LUKS multi-key enrolment at format time**: `additionalKeyFiles`, `extraOpenArgs`, `initrdUnlock` (`luks.nix:92,109,98`). LIS `unlock[]` is an ordered *list*, but there is no way to attach distinct key material to each entry.\n- **btrfs swapfile inside a subvolume** (`btrfs.nix:125,138` `swapType`): LIS `storage.swap.file.path` is an absolute path with no notion of which subvolume hosts it, which is exactly the information btrfs needs.\n- **Nix-specific everything** — flake vs channel, `nix.settings`, overlays, home-manager, binary-cache public keys (the applier emits `substituters` at `:822` with no `trusted-public-keys`). Spec §18 parks this in `x-nixos`, which this applier ignores entirely (`patternProperties ^x-` are skipped by `check_unhandled`).",
      "## Verification base\n\n- Applier: `/home/bresilla/lis/tools/appliers/lis2alpine.py` + `/home/bresilla/lis/tools/appliers/lis_common.py`\n- Installer vocabulary: a real Alpine tree at `/tmp/oslo-alpine-vm/distro` — `alpine-release` 3.24.1, `usr/sbin/setup-alpine` says `VERSION=3.22.0-r0`. The e2e ISO is Alpine 3.21.3 (`tools/e2e/iso.py:10`); **I could not inspect 3.21's alpine-conf**, so any claim about setup-* behaviour below is verified against 3.22.0-r0 only.\n- I ran the applier on nine hand-built documents (max-field, plain/simple, esp+boot, nvme, percent-size, luks1+lvm, two-user, `$`-in-script) plus the repo's own recipes.\n\n---\n\n## A. SILENT DROPS (declared, read, neither emitted nor refused)\n\n**A1. Swap is never enabled — in every recipe in the repo.** `prepare_script` emits `mkswap` (`lis2alpine.py:540`, `:562`) and nothing more. In mountpoint mode setup-disk builds fstab from `genfstab -U \"$mnt\"` (`setup-disk:713`) plus `$SWAP_DEVICES` (`setup-disk:714-720`) — an env var the applier never sets. Result: a formatted swap area absent from fstab and never `swapon`ed. `test-full-install`, `test-encrypted`, `test-lvm`, `test-lvm-btrfs`, `test-raid` all declare swap; `tools/e2e/verifier.py` has no swap check, which is why this passes.\n\n**A2. Any mountpoint other than `/` and `/boot` is formatted and thrown away.** `prepare_script` mounts only `$rootdev`, the root's btrfs subvolumes, and `bootdev` (`lis2alpine.py:573-598`). A partition or LVM volume with `mountpoint: /home`, `/var`, `/doc` is `mkfs`'d (`:536-545`, `:563-567`) and never mounted, so `genfstab` never sees it. Reproduced: `/home` xfs partition (max doc) and `lv doc` in `docs/examples/server-lvm-pool.lis.json`. `mountpoint` *is* read (`role_mountpoint`), so `check_unread` says nothing.\n\n**A3. With both an ESP and a separate `/boot`, one of them vanishes and the other is mounted in the wrong place.** `boot_part = next(p for p in partitions if role_mountpoint(p) in (\"/boot\",\"/boot/efi\"))` (`:159-161`). Reproduced with `esp`+`boot`+`root`: the ESP became `bootdev` and was mounted at `$mnt/boot`; the declared ext4 `/boot` partition was `mkfs.ext4`'d and abandoned. The ESP's declared `/boot/efi` silently becomes `/boot` in fstab.\n\n**A4. On the simple path the entire storage and boot description is discarded.** When there are no subvolumes/raid/lvm/encryption, `manual` is false (`:165-167`) and the only storage output is `DISKOPTS=\"-m sys /dev/vda\"` (`:216`). The `env` list holding `BOOTLOADER`, `USE_EFI`, `DISKLABEL`, `KERNELOPTS`, `ROOTFS`, `BOOTFS` (`:169-207`) is passed to `prepare_script` only, i.e. dropped. Even if written into the answerfile it would not reach setup-disk: setup-alpine sources the file (`setup-alpine`, `. \"$USEANSWERFILE\"`) and runs `setup-disk` as a child without exporting. setup-disk then uses its own defaults: `ROOTFS=ext4`, `BOOTFS=ext4`, `DISKLABEL=dos`, `KERNELOPTS=quiet` (`setup-disk:9-13`), `BOOT_SIZE=300M`, RAM-derived `SWAP_SIZE`, maximised root (`setup-disk:1457-1459`, `:1836-1837`), and \"the default bootloader is syslinux(extlinux) except when EFI is detected\" (`setup-disk:1654-1655`). Verified with `plain.lis.json` (`fs: xfs`, `loader: grub`, `firmware: uefi`, 3 sized partitions): the answerfile contains none of it, and only `partitions[].size`/`label` produced tracker warnings — `fs`, `role`, `mountpoint`, `target.firmware` and `boot.loader` are read elsewhere and so pass silently.\n\n**A5. Only one SSH key, only for one user.** `USERSSHKEY=\"{keys[0]}\"` (`:229`). `users['root'].ssh_authorized_keys`, secondary users' keys, and every key after the first are never emitted. The tracker cannot see it: `declared_paths` collapses indices to `users[].ssh_authorized_keys`, so reading it for the primary marks it read for all. setup-alpine has a dedicated `ROOTSSHKEY` answer key that is never used.\n\n**A6. `admin: true` on a non-primary user is dropped.** `admin` is read only for `normal[0]` (`:222`); the secondary-user loop (`:297-311`) creates the account, sets a password and groups, and never touches `admin` or wheel/doas. Same collapsed-path blindness as A5. Verified with a two-admin document.\n\n**A7. `users[].comment` for the primary user is dropped.** Emitted as `chroot chfn -f ... || true` (`:293-295`). There is no `chfn` in the Alpine tree (`find . -name chfn` → nothing; busybox provides `adduser/addgroup/chpasswd/passwd` only), so the guard swallows it.\n\n**A8. `storage.encryption[].type: luks1` becomes luks2.** `--type luks2` is hardcoded (`:469`); `container.get(\"type\")` is never read. Only the generic \"declared but never read\" tracker warning fires.\n\n**A9. An LVM volume without `fs` is neither formatted nor refused.** `if fs := volume.get(\"fs\")` (`:536`); the schema requires only `name`+`size` for volumes, and there is no role-default fallback as there is for partitions. A `mountpoint: /` volume with no `fs` yields `rootdev=` pointing at an unformatted LV.\n\n**A10. Percent sizes are silently mis-computed.** `cumulative()` (`:663-672`) only understands MiB/GiB/TiB, so `size: \"50%\"` contributes 0: reproduced `mkpart primary 1MiB 1MiB`. For LVM, `-L {size.replace('iB','')}` (`:533`) turns `\"50%\"` into `lvcreate -L 50%`, which is not valid (percent needs `-l`). `sizeOrRest` explicitly allows `%` (`schema.json:1253-1256`). No warning, no refusal.\n\n**A11. `software.services.enable` is not translated and has no error path.** `rc-update add {unit} default` (`:314`), unguarded, while `disable` gets `|| true` (`:316`). SPEC §11 says services use systemd unit names and \"non-systemd appliers translate\" — this one passes them through verbatim, so `NetworkManager`, `systemd-timesyncd`, `bluetooth` etc. abort the whole post script under `set -e`.\n\n*Warned, therefore acceptable but worth knowing:* `system.init` (a document asking for systemd gets only a generic tracker warning, not a refusal on an OpenRC-only distro), `storage.swap.zram/.file`, `network.hosts`, `network.ssh.password_auth/permit_root`, `network.firewall.*`, `boot.os_prober/secure_boot/uki/initramfs/password_hash/console`, `boot.kernel.modules/blacklist`, `storage.raid[].spares`, `partitions[].label/mount_options`, `subvolumes[].mount_options`, `system.domain/kdump/telemetry/time.servers/keymap.font`, `mirror.country`, all of `installer.*`, `users[].scripts.post`. Note `storage.wipe: false` warns (`:44-46`) but the applier still runs `parted mkpart` + `mkfs`, where SPEC §6.1 says it MUST fail.\n\n---\n\n## B. DRIFT (emitted config that does not match the document)\n\n**B1. `usermod` does not exist on Alpine, and the post script dies there.** `chroot \"$target\" usermod -p ... root` (`:266`) and the secondary-user form (`:309`). No `usermod` anywhere in the Alpine tree (only `shadow` provides it; `usr/sbin/{adduser,addgroup,chpasswd}` are busybox symlinks). `lis_common.uid_commands` already documents this (\"Alpine's install repo has no shadow package, so there is no usermod applet\", `lis_common.py:738-740`) — the knowledge exists and lis2alpine does not use it. With `set -e` (`:234`), a document that gives root a password hash or declares a second user aborts lis-post.sh at that line, so users, files, sudoers, uid fixups, hooks and the birth certificate are all skipped. The e2e recipe has neither, which is why it is not caught.\n\n**B2. `json.dumps()` double-quoting lets the installer host expand what should reach the target verbatim.** `sh -c {json.dumps(...)}` at `:323`, `:325`, `:327`, `:329`, `:331`, `:333`, `:338`, `:342`, and `usermod -p {json.dumps(field)}` at `:309`. Three consequences, all reproduced:\n- Script bodies are rewritten: `scripts.post_install` containing `echo \"host=$(hostname) home=$HOME\"` was emitted inside double quotes; run through a shell it becomes `echo \"host=tron home=/root/installerhome\"` — the *installer's* values, evaluated on the installer, before the chroot. Backticks likewise. `firstboot` scripts use base64 (`:354-358`) and `prepare_script` uses `shquote`, so the inconsistency is internal.\n- `usermod -p \"$6$salt$XXyyZZ\" bob` expands to `usermod -p  bob` — an empty password field, i.e. a passwordless account, had `usermod` existed. The root and primary paths correctly use `shquote` (`:266`, `:286`).\n- `enrollment_commands` output (`lis_common.py:825-827`) is emitted as `chroot \"$target\" sh -c \"dev=$(blkid -t TYPE=crypto_LUKS -o device|head -n1); ... PASSWORD=$(cat /run/lis/seed/secrets/luks-cr.key) ...\"`. Both substitutions run on the host, so the LUKS passphrase is interpolated into an argv, and `blkid -t/-o` are util-linux options busybox ignores — the very hazard `seed_mount_commands` was written to avoid (`lis_common.py:966-975`).\n\n**B3. `sudo: nopasswd` writes a sudoers drop-in into a system that has doas and no sudo.** `sudoers_commands` (`lis_common.py:252-269`) is emitted at `:322-323`; `sudo` is never added to the package list (`:236-257`), and setup-alpine's `-a` installs **doas** with `permit persist :wheel` (`setup-user`, admin branch). The account still gets prompted. The e2e check passes anyway because it accepts `cat /etc/sudoers.d/99-lis-<name>` as evidence (`verifier.py:270-278`).\n\n**B4. A non-admin primary user is put into wheel regardless.** `USEROPTS=\"{admin}-g wheel {name}\"` (`:224`) — `-g wheel` is emitted unconditionally; only `-a` is conditional. The document's `admin: false` still yields wheel membership.\n\n**B5. A serial console is configured that no document asked for, with hardcoded values.** `:645-648` always writes `GRUB_TERMINAL=\"serial console\"` and `GRUB_SERIAL_COMMAND=\"serial --unit=0 --speed=115200\"`, while `check_boot_extras` warns \"boot.console is not applied\". A document declaring `console.serial: \"ttyS1,9600n8\"` gets ttyS0/115200. The same `printf` overwrites `/etc/default/grub` wholesale, discarding what setup-disk had just written there — `GRUB_TIMEOUT=${BOOT_TIMEOUT}`, `GRUB_DISABLE_SUBMENU`, `GRUB_DISABLE_RECOVERY` and `grub_serial_config` (`setup-disk:355-371`) — and when `boot.timeout` is absent, GRUB_TIMEOUT disappears entirely.\n\n**B6. Invented networking.** `INTERFACESOPTS` is a fixed `eth0 … dhcp` block and `DNSOPTS=\"-n 1.1.1.1\"` (`:75-77`) — a specific public resolver the document never named. It is currently inert (setup-alpine only calls `setup-dns` when no dhcp iface exists) but becomes live the moment INTERFACESOPTS gains static addressing.\n\n**B7. `NTPOPTS=\"chrony\"` is emitted even when `system.time` is absent** (`:87-95`, default `provider=\"auto\"` → chrony). Alpine's own default is busybox ntpd (`setup-ntp`, `ask … busybox`). Also `network.manager: networkmanager` installs and enables NetworkManager (`lis_common.py:918-926`) on top of the `networking`/`/etc/network/interfaces` setup the answerfile just created.\n\n**B8. NVMe/eMMC partition nodes are wrong.** `f\"{device}{index}\"` (`:449`). Reproduced with `/dev/nvme0n1` → `/dev/nvme0n11`, `/dev/nvme0n12`. `target.disks[].match.type: \"nvme\"` is a schema value and `resolve_disk_paths` can select such a device (`lis_common.py:1037-1040`), so this is reachable; it fails hard at `mkfs`.\n\n**B9. `size: \"rest\"` is not ordered last for LVM.** Volumes are created in document order (`:531-534`); a `rest` volume declared before a sized one emits `lvcreate -l 100%FREE` first and the next `lvcreate -L 1G` fails. Reproduced.\n\n**B10. `x-*` extensions are refused.** `check_unhandled(doc, ALL_SECTIONS)` (`:697`, `lis_common.py:102-108`) has no exemption for `x-`. Verified: `refused: section 'x-alpine' is declared but this applier does not translate it`. SPEC §2.2 and §18 say these MUST be ignored, and §11 points at `x-*` as the place distro-specific repos and packages belong — so the escape hatch is unusable with any applier built on this helper.\n\n**B11. `desktop.display_manager: \"none\"` is refused** (`lis_common.py:911-913` — `DISPLAY_MANAGERS` has no `none`) even though it is a schema enum value meaning \"install no DM\", which Alpine satisfies by doing nothing. Verified in the max run.\n\n**B12. tpm2/fido2 unlock emits `systemd-cryptenroll` on a systemd-less distro** (`:327` via `lis_common.py:790-828`), guarded by `|| true`, so the declared unlock method quietly never exists. The applier does warn separately (`:130-131`).\n\n---\n\n## C. UNUSED INSTALLER CAPABILITY (expressible in LIS today)\n\n1. **`network.wifi` — refused at `:69` as \"not expressible in a setup-alpine answerfile\", which is not true.** setup-alpine has `WIFIOPTS` and calls `setup-interfaces -w \"$WIFIOPTS\"` (`setup-alpine`, `run_setup_interfaces`), taking a wpa_supplicant.conf the applier could generate from `ssid`/`psk_hash`/`hidden`.\n2. **`network.interfaces` — refused at `:74`.** `INTERFACESOPTS` is the literal contents of `/etc/network/interfaces`; static `address`/`gateway` and per-interface names are directly expressible, and `DNSOPTS` accepts nameservers positionally (`setup-dns` usage).\n3. **`system.domain`** — `setup-dns -d DOMAINNAME` (`setup-dns:13, :22-27`). Currently only a tracker warning.\n4. **Community repository.** `setup-apkrepos` writes the community line commented out unless `-c` (`setup-apkrepos:186, :148, :174`); the applier emits `APKREPOSOPTS=\"-1\"` (`:82`). Most of what the applier itself then installs — `firefox`, `gnome`, `plasma`, `xfce4` from `:250-253` — lives in community, so `apk add` fails and (per `set -e`) takes the rest of the post script with it.\n5. **`setup-desktop`** supports `gnome|plasma|xfce|xfce-wayland|mate|sway|lxqt` and wires up the display manager, dbus/elogind/greetd and an xorg or wayland base (`setup-desktop:51, :63-115`). The applier instead `apk add`s a bare metapackage and refuses `desktop:sway` / `desktop:hyprland` as having \"no Alpine package set\" (`:254-255`) — sway is supported by setup-desktop.\n6. **`network.ssh.password_auth` / `permit_root`** — `setup-sshd` has `set_sshd_config` for exactly these keys; a post-install `sed` on `sshd_config` is trivial. Currently tracker-warned only.\n7. **setup-disk's whole env/flag surface on the simple path**: `ROOTFS`, `BOOTFS`, `VARFS`, `DISKLABEL`, `KERNELOPTS`, `BOOT_TIMEOUT`, `USE_EFI`, `SWAP_SIZE`/`ROOT_SIZE`/`BOOT_SIZE`, `SWAP_DEVICES`, `-B BOOTLOADER`, `-b DISKDEV`, `-k kernelflavor` (`setup-disk:9-14, 1636-1671`). The `--apply` driver runs setup-alpine as a subprocess (`:745`) and could export these; today they are computed and dropped (A4).\n8. **`users['root'].ssh_authorized_keys`** — setup-alpine's `ROOTSSHKEY` answer key exists and is never emitted.\n9. **Non-x86 targets.** setup-disk installs `arm64-efi`/`riscv64-efi` grub (`setup-disk:317-326`); the applier hardcodes `x86_64-efi`/`i386-pc` (`:652-653`) and correctly refuses other arches (`:723`) — capability left on the table rather than a bug.\n\n---\n\n## D. SPEC GAPS (Alpine can do it, the schema cannot say it)\n\n1. **Device manager.** `DEVDOPTS` → `setup-devd mdev|mdevd|udev` (`setup-devd:12, :20-24`). No LIS field. Not cosmetic: the applier's hardcoded `eth0` in `INTERFACESOPTS` is only correct under mdev.\n2. **Diskless / lbu / apk cache.** `LBUOPTS`, `APKCACHEOPTS` and `setup-disk -m data` are Alpine's defining install modes; the applier hardcodes `\"none\"` (`:217-218`) because LIS has no way to ask for them.\n3. **doas.** `users[].sudo` is `default|nopasswd`, a sudo-shaped enum; Alpine's admin mechanism is doas with `permit persist :wheel`. There is no way to express \"doas, no password\".\n4. **Repository components.** apk `community`/`testing`/edge selection has no schema field; SPEC §11 delegates it to `x-*`, which is refused (B10).\n5. **Kernel flavors.** `boot.kernel.variant` is `default|lts|hardened|realtime|zen`; Alpine ships `lts`, `virt`, `edge` and `setup-disk -k` takes any of them. `virt` — the right choice for the QEMU targets this repo tests — cannot be requested.\n6. **Bootloaders.** setup-disk supports `syslinux|grub|limine|u-boot|zipl|raspberrypi-bootloader|none` (`setup-disk:1656`); `boot.loader` is `systemd-boot|grub|auto`. The applier's own map at `:171-172` lists `syslinux`, `limine` and `none` — values a valid document can never contain.\n7. **musl/locale.** `system.locale` and `extra_locales` have no meaning on musl; both are warned (`:62-64`, `lis_common.py:626-629`). A `locale: none`-style acknowledgement, or a per-applier \"not applicable\" channel distinct from `warn`, would let the document say what it means."
    ]
  },
  "workflowProgress": [
    {
      "type": "workflow_phase",
      "index": 1,
      "title": "Audit"
    },
    {
      "type": "workflow_phase",
      "index": 2,
      "title": "Synthesize"
    },
    {
      "type": "workflow_agent",
      "index": 1,
      "label": "audit:ubuntu",
      "phaseIndex": 1,
      "phaseTitle": "Audit",
      "agentId": "a1bc0af1299296d93",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1785664980911,
      "queuedAt": 1785664974651,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /home/bresilla/lis/tools/appliers && grep -n \"shell_pack…",
      "promptPreview": "LIS project at /home/bresilla/lis. An applier translates a declarative LIS document into a distro\ninstaller's native configuration. The spec is in spec/schema.json, spec/schema.md and\nspec/delivery.md. Appliers are in tools/appliers/.\n\nTHIS IS A MAPPING EXERCISE, NOT A FIX. Do NOT edit any file. Report findings only.\n\nThe governing principle (SPEC §2.3): an applier MUST refuse a document containin…",
      "lastProgressAt": 1785665597695,
      "tokens": 141839,
      "toolCalls": 29,
      "durationMs": 616784,
      "resultPreview": "I read the applier, the spec, `lis_common.py`, ran it against the e2e recipes and a maximal document, and checked the subiquity autoinstall schema + curtin config docs. All line refs are `/home/bresilla/lis/tools/appliers/lis2autoinstall.py` unless noted.\n\n---\n\n# A. SILENT DROPS — declared, neither emitted nor refused\n\n**A1. `software.role: \"minimal\"` / `\"server\"` — completely silent.**\n`role_pack…"
    },
    {
      "type": "workflow_agent",
      "index": 2,
      "label": "audit:debian",
      "phaseIndex": 1,
      "phaseTitle": "Audit",
      "agentId": "ad6499086cb7fe5d6",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1785664985262,
      "queuedAt": 1785664974651,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "S=/tmp/nix-shell.ouGjCL/claude-1000/-home-bresilla-lis/6787…",
      "promptPreview": "LIS project at /home/bresilla/lis. An applier translates a declarative LIS document into a distro\ninstaller's native configuration. The spec is in spec/schema.json, spec/schema.md and\nspec/delivery.md. Appliers are in tools/appliers/.\n\nTHIS IS A MAPPING EXERCISE, NOT A FIX. Do NOT edit any file. Report findings only.\n\nThe governing principle (SPEC §2.3): an applier MUST refuse a document containin…",
      "lastProgressAt": 1785666175998,
      "tokens": 211170,
      "toolCalls": 118,
      "durationMs": 1190736,
      "resultPreview": "I verified everything against the bookworm udebs (fetched to `/tmp/dibits/x/…`) plus the existing clones at `/tmp/pauto`, `/tmp/pax` (partman-base), `/tmp/palvm`, `/tmp/pcrypto`, and `doc/devel/partman-auto-recipe.txt` (`/tmp/dibits/par.txt`). Test documents used: `/home/bresilla/lis/docs/examples/*` plus four I wrote in the scratchpad (`deb-max.lis.json`, `two.lis.json`, `rootkey.lis.json`, `sv.l…"
    },
    {
      "type": "workflow_agent",
      "index": 3,
      "label": "audit:fedora",
      "phaseIndex": 1,
      "phaseTitle": "Audit",
      "agentId": "a2a9eec7af6bececc",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1785664990636,
      "queuedAt": 1785664974651,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "P=/tmp/f41root/usr/lib/python3.13/site-packages/pykickstart…",
      "promptPreview": "LIS project at /home/bresilla/lis. An applier translates a declarative LIS document into a distro\ninstaller's native configuration. The spec is in spec/schema.json, spec/schema.md and\nspec/delivery.md. Appliers are in tools/appliers/.\n\nTHIS IS A MAPPING EXERCISE, NOT A FIX. Do NOT edit any file. Report findings only.\n\nThe governing principle (SPEC §2.3): an applier MUST refuse a document containin…",
      "lastProgressAt": 1785665877155,
      "tokens": 161693,
      "toolCalls": 65,
      "durationMs": 886519,
      "resultPreview": "## Environment note\n\nAll installer citations below are read from the Fedora 41 `install.img` tree already extracted at `/tmp/f41root` (only `pykickstart`, `pyanaconda`, `blivet` are present — no comps/`.treeinfo`, so group names could not be verified). Kickstart output was re-parsed with the real F41 handler (`pykickstart.version.makeVersion(\"F41\")`) to check what Anaconda actually keeps. Probe do…"
    },
    {
      "type": "workflow_agent",
      "index": 4,
      "label": "audit:suse",
      "phaseIndex": 1,
      "phaseTitle": "Audit",
      "agentId": "a64450a42cc555b0c",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1785664980520,
      "queuedAt": 1785664974651,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "S=/tmp/nix-shell.ouGjCL/claude-1000/-home-bresilla-lis/6787…",
      "promptPreview": "LIS project at /home/bresilla/lis. An applier translates a declarative LIS document into a distro\ninstaller's native configuration. The spec is in spec/schema.json, spec/schema.md and\nspec/delivery.md. Appliers are in tools/appliers/.\n\nTHIS IS A MAPPING EXERCISE, NOT A FIX. Do NOT edit any file. Report findings only.\n\nThe governing principle (SPEC §2.3): an applier MUST refuse a document containin…",
      "lastProgressAt": 1785665582969,
      "tokens": 130362,
      "toolCalls": 33,
      "durationMs": 602449,
      "resultPreview": "## A. SILENT DROPS — declared, neither emitted nor reported\n\nAll verified by running the applier and reading the two generated files. In each case the field *is* read somewhere, so `check_unread()` stays quiet, and no `warn()`/`refuse()` covers it.\n\n**A1. The whole `storage.encryption` section vanishes from the Agama profile.**\n`agama_drives()` (`tools/appliers/lis2agama.py:286-342`) consults `sto…"
    },
    {
      "type": "workflow_agent",
      "index": 5,
      "label": "audit:arch",
      "phaseIndex": 1,
      "phaseTitle": "Audit",
      "agentId": "a233c5671fd4f55ca",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1785664978737,
      "queuedAt": 1785664974651,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /tmp/archx/full/usr/lib/python3.14/site-packages/archins…",
      "promptPreview": "LIS project at /home/bresilla/lis. An applier translates a declarative LIS document into a distro\ninstaller's native configuration. The spec is in spec/schema.json, spec/schema.md and\nspec/delivery.md. Appliers are in tools/appliers/.\n\nTHIS IS A MAPPING EXERCISE, NOT A FIX. Do NOT edit any file. Report findings only.\n\nThe governing principle (SPEC §2.3): an applier MUST refuse a document containin…",
      "lastProgressAt": 1785665685828,
      "tokens": 182319,
      "toolCalls": 52,
      "durationMs": 707090,
      "resultPreview": "All archinstall citations are relative to `/tmp/archx/full/usr/lib/python3.14/site-packages/archinstall/` (4.4 from the ISO). Applier citations are `/home/bresilla/lis/tools/appliers/lis2archinstall.py` unless noted.\n\n---\n\n# A. SILENT DROPS\n\n### A1. `storage.raid` + `storage.encryption` ⇒ the disk is installed unencrypted, no refusal\n`translate()` branches at :570 — if `storage.raid` is present it…"
    },
    {
      "type": "workflow_agent",
      "index": 6,
      "label": "audit:nixos",
      "phaseIndex": 1,
      "phaseTitle": "Audit",
      "agentId": "aa207b60916ad017b",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1785664982796,
      "queuedAt": 1785664974651,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /tmp/nix-shell.ouGjCL/claude-1000/-home-bresilla-lis/678…",
      "promptPreview": "LIS project at /home/bresilla/lis. An applier translates a declarative LIS document into a distro\ninstaller's native configuration. The spec is in spec/schema.json, spec/schema.md and\nspec/delivery.md. Appliers are in tools/appliers/.\n\nTHIS IS A MAPPING EXERCISE, NOT A FIX. Do NOT edit any file. Report findings only.\n\nThe governing principle (SPEC §2.3): an applier MUST refuse a document containin…",
      "lastProgressAt": 1785665726315,
      "tokens": 161738,
      "toolCalls": 38,
      "durationMs": 743519,
      "resultPreview": "Verified against disko `de57087` (`/nix/store/z2bsz1xw44k3gbkb4ks4ds8p0gyspvwb-source`) and nixpkgs 25.05 (`/nix/store/3p306srz83h9z9v0ma9xcxb8y8cdxkxj-source`) — the release `lis2nixos` pins in `system.stateVersion`. All applier paths are `/home/bresilla/lis/tools/appliers/lis2nixos.py` unless noted.\n\n---\n\n# A. SILENT DROPS — declared, neither emitted nor refused\n\n**A1. `software.exclude` — dropp…"
    },
    {
      "type": "workflow_agent",
      "index": 7,
      "label": "audit:alpine",
      "phaseIndex": 1,
      "phaseTitle": "Audit",
      "agentId": "acdd51b36ab2a233f",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1785664982472,
      "queuedAt": 1785664974651,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /tmp/oslo-alpine-vm/distro/usr/sbin && sed -n '1,40p' se…",
      "promptPreview": "LIS project at /home/bresilla/lis. An applier translates a declarative LIS document into a distro\ninstaller's native configuration. The spec is in spec/schema.json, spec/schema.md and\nspec/delivery.md. Appliers are in tools/appliers/.\n\nTHIS IS A MAPPING EXERCISE, NOT A FIX. Do NOT edit any file. Report findings only.\n\nThe governing principle (SPEC §2.3): an applier MUST refuse a document containin…",
      "lastProgressAt": 1785665788942,
      "tokens": 183595,
      "toolCalls": 45,
      "durationMs": 806470,
      "resultPreview": "## Verification base\n\n- Applier: `/home/bresilla/lis/tools/appliers/lis2alpine.py` + `/home/bresilla/lis/tools/appliers/lis_common.py`\n- Installer vocabulary: a real Alpine tree at `/tmp/oslo-alpine-vm/distro` — `alpine-release` 3.24.1, `usr/sbin/setup-alpine` says `VERSION=3.22.0-r0`. The e2e ISO is Alpine 3.21.3 (`tools/e2e/iso.py:10`); **I could not inspect 3.21's alpine-conf**, so any claim ab…"
    },
    {
      "type": "workflow_agent",
      "index": 8,
      "label": "synthesis",
      "phaseIndex": 2,
      "phaseTitle": "Synthesize",
      "agentId": "a91f1741ec8bda8d6",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1785666192942,
      "queuedAt": 1785666177683,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "grep -n \"/home/\" tools/appliers/lis_common.py | head -20; e…",
      "promptPreview": "LIS project at /home/bresilla/lis. An applier translates a declarative LIS document into a distro\ninstaller's native configuration. The spec is in spec/schema.json, spec/schema.md and\nspec/delivery.md. Appliers are in tools/appliers/.\n\nTHIS IS A MAPPING EXERCISE, NOT A FIX. Do NOT edit any file. Report findings only.\n\nThe governing principle (SPEC §2.3): an applier MUST refuse a document containin…",
      "lastProgressAt": 1785666415096,
      "tokens": 73616,
      "toolCalls": 4,
      "durationMs": 222154,
      "resultPreview": "# LIS APPLIER AUDIT — CONSOLIDATED MAP\n\nSeven per-applier audits merged. I independently re-verified every `lis_common.py` claim cited below (grep/read of the actual source); per-applier and per-installer claims are carried through from the individual audits with evidence quality flagged at the end.\n\n---\n\n## 1. CROSS-CUTTING — fix in `lis_common.py` or the spec, not seven times\n\n**X1. Encryption d…"
    }
  ],
  "totalTokens": 1246332,
  "totalToolCalls": 384
}