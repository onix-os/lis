#!/usr/bin/env python3
"""lis-test-e2e — Standalone Automated VM Installation & Verification Engine.

Usage: lis-test-e2e --distro nixos|ubuntu|arch|fedora|suse|debian|alpine|all [--iso ISO_PATH] [--recipe RECIPE.lis.json]
"""

import pathlib
import sys

# Ensure repository root and tools/ directory are on PYTHONPATH
repo_root = pathlib.Path(__file__).resolve().parent.parent
sys.path.insert(0, str(repo_root))

from tools.e2e.main import main

if __name__ == "__main__":
    sys.exit(main())
